# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.


# ── namespaces introduced by this pack ──
def "nsdef:atr" "atr — the atr namespace."
set.add name="namespaces" id="nsdef:atr"
def "nsdef:comm" "comm — the comm namespace."
set.add name="namespaces" id="nsdef:comm"
def "nsdef:fact" "fact — the fact namespace."
set.add name="namespaces" id="nsdef:fact"
def "nsdef:game" "game — the game namespace."
set.add name="namespaces" id="nsdef:game"
def "nsdef:geostat" "geostat — the geostat namespace."
set.add name="namespaces" id="nsdef:geostat"
def "nsdef:journey" "journey — the journey namespace."
set.add name="namespaces" id="nsdef:journey"
def "nsdef:legalhist" "legalhist — the legalhist namespace."
set.add name="namespaces" id="nsdef:legalhist"
def "nsdef:mystic" "mystic — the mystic namespace."
set.add name="namespaces" id="nsdef:mystic"
def "nsdef:product" "product — the product namespace."
set.add name="namespaces" id="nsdef:product"
def "nsdef:uinf" "uinf — the uinf namespace."
set.add name="namespaces" id="nsdef:uinf"

# ── relations introduced by this pack ──
def "reldef:@adapts" "adapts — 'adapts' relation asserted between two atoms."
set.add name="rels" id="reldef:@adapts"
def "reldef:@adjusts_for" "adjusts for — 'adjusts for' relation asserted between two atoms."
set.add name="rels" id="reldef:@adjusts_for"
def "reldef:@affects" "affects — 'affects' relation asserted between two atoms."
set.add name="rels" id="reldef:@affects"
def "reldef:@also_belongs_to" "also belongs to — 'also belongs to' relation asserted between two atoms."
set.add name="rels" id="reldef:@also_belongs_to"
def "reldef:@alters" "alters — 'alters' relation asserted between two atoms."
set.add name="rels" id="reldef:@alters"
def "reldef:@applies_to" "applies to — 'applies to' relation asserted between two atoms."
set.add name="rels" id="reldef:@applies_to"
def "reldef:@bound_by" "bound by — 'bound by' relation asserted between two atoms."
set.add name="rels" id="reldef:@bound_by"
def "reldef:@can_drive" "can drive — 'can drive' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_drive"
def "reldef:@can_lead_to" "can lead to — 'can lead to' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_lead_to"
def "reldef:@can_manifest_as" "can manifest as — 'can manifest as' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_manifest_as"
def "reldef:@can_overlap_with" "can overlap with — 'can overlap with' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_overlap_with"
def "reldef:@causes" "causes — 'causes' relation asserted between two atoms."
set.add name="rels" id="reldef:@causes"
def "reldef:@claims" "claims — 'claims' relation asserted between two atoms."
set.add name="rels" id="reldef:@claims"
def "reldef:@cognate_of" "cognate of — 'cognate of' relation asserted between two atoms."
set.add name="rels" id="reldef:@cognate_of"
def "reldef:@collapses_into" "collapses into — 'collapses into' relation asserted between two atoms."
set.add name="rels" id="reldef:@collapses_into"
def "reldef:@concerns" "concerns — 'concerns' relation asserted between two atoms."
set.add name="rels" id="reldef:@concerns"
def "reldef:@controls" "controls — 'controls' relation asserted between two atoms."
set.add name="rels" id="reldef:@controls"
def "reldef:@defines" "defines — 'defines' relation asserted between two atoms."
set.add name="rels" id="reldef:@defines"
def "reldef:@describes" "describes — 'describes' relation asserted between two atoms."
set.add name="rels" id="reldef:@describes"
def "reldef:@evokes" "evokes — 'evokes' relation asserted between two atoms."
set.add name="rels" id="reldef:@evokes"
def "reldef:@excludes" "excludes — 'excludes' relation asserted between two atoms."
set.add name="rels" id="reldef:@excludes"
def "reldef:@exerts" "exerts — 'exerts' relation asserted between two atoms."
set.add name="rels" id="reldef:@exerts"
def "reldef:@exhibits_extreme" "exhibits extreme — 'exhibits extreme' relation asserted between two atoms."
set.add name="rels" id="reldef:@exhibits_extreme"
def "reldef:@expresses" "expresses — 'expresses' relation asserted between two atoms."
set.add name="rels" id="reldef:@expresses"
def "reldef:@fails" "fails — 'fails' relation asserted between two atoms."
set.add name="rels" id="reldef:@fails"
def "reldef:@frequently_utilizes" "frequently utilizes — 'frequently utilizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@frequently_utilizes"
def "reldef:@generates" "generates — 'generates' relation asserted between two atoms."
set.add name="rels" id="reldef:@generates"
def "reldef:@governed_by" "governed by — 'governed by' relation asserted between two atoms."
set.add name="rels" id="reldef:@governed_by"
def "reldef:@historically_relies_on" "historically relies on — 'historically relies on' relation asserted between two atoms."
set.add name="rels" id="reldef:@historically_relies_on"
def "reldef:@introduces" "introduces — 'introduces' relation asserted between two atoms."
set.add name="rels" id="reldef:@introduces"
def "reldef:@lacks" "lacks — 'lacks' relation asserted between two atoms."
set.add name="rels" id="reldef:@lacks"
def "reldef:@literalizes" "literalizes — 'literalizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@literalizes"
def "reldef:@located_in" "located in — 'located in' relation asserted between two atoms."
set.add name="rels" id="reldef:@located_in"
def "reldef:@may_be_answered_by" "may be answered by — 'may be answered by' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_be_answered_by"
def "reldef:@may_cause" "may cause — 'may cause' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_cause"
def "reldef:@may_enable" "may enable — 'may enable' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_enable"
def "reldef:@may_include" "may include — 'may include' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_include"
def "reldef:@may_lead_to" "may lead to — 'may lead to' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_lead_to"
def "reldef:@may_require" "may require — 'may require' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_require"
def "reldef:@names_part_of" "names part of — 'names part of' relation asserted between two atoms."
set.add name="rels" id="reldef:@names_part_of"
def "reldef:@namesake_of" "namesake of — 'namesake of' relation asserted between two atoms."
set.add name="rels" id="reldef:@namesake_of"
def "reldef:@observes" "observes — 'observes' relation asserted between two atoms."
set.add name="rels" id="reldef:@observes"
def "reldef:@observes_without_participating" "observes without participating — 'observes without participating' relation asserted between two atoms."
set.add name="rels" id="reldef:@observes_without_participating"
def "reldef:@obsessed_with" "obsessed with — 'obsessed with' relation asserted between two atoms."
set.add name="rels" id="reldef:@obsessed_with"
def "reldef:@often_ends_in" "often ends in — 'often ends in' relation asserted between two atoms."
set.add name="rels" id="reldef:@often_ends_in"
def "reldef:@often_takes_form_of" "often takes form of — 'often takes form of' relation asserted between two atoms."
set.add name="rels" id="reldef:@often_takes_form_of"
def "reldef:@orbits" "orbits — 'orbits' relation asserted between two atoms."
set.add name="rels" id="reldef:@orbits"
def "reldef:@parallels" "parallels — 'parallels' relation asserted between two atoms."
set.add name="rels" id="reldef:@parallels"
def "reldef:@passes" "passes — 'passes' relation asserted between two atoms."
set.add name="rels" id="reldef:@passes"
def "reldef:@personifies" "personifies — 'personifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@personifies"
def "reldef:@possesses" "possesses — 'possesses' relation asserted between two atoms."
set.add name="rels" id="reldef:@possesses"
def "reldef:@quantifies" "quantifies — 'quantifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@quantifies"
def "reldef:@reacts_against" "reacts against — 'reacts against' relation asserted between two atoms."
set.add name="rels" id="reldef:@reacts_against"
def "reldef:@reads_as" "reads as — 'reads as' relation asserted between two atoms."
set.add name="rels" id="reldef:@reads_as"
def "reldef:@referenced_by" "referenced by — 'referenced by' relation asserted between two atoms."
set.add name="rels" id="reldef:@referenced_by"
def "reldef:@reorients_toward" "reorients toward — 'reorients toward' relation asserted between two atoms."
set.add name="rels" id="reldef:@reorients_toward"
def "reldef:@resolved_by" "resolved by — 'resolved by' relation asserted between two atoms."
set.add name="rels" id="reldef:@resolved_by"
def "reldef:@reveals" "reveals — 'reveals' relation asserted between two atoms."
set.add name="rels" id="reldef:@reveals"
def "reldef:@sources" "sources — 'sources' relation asserted between two atoms."
set.add name="rels" id="reldef:@sources"
def "reldef:@then" "then — 'then' relation asserted between two atoms."
set.add name="rels" id="reldef:@then"
def "reldef:@underpins" "underpins — 'underpins' relation asserted between two atoms."
set.add name="rels" id="reldef:@underpins"
def "reldef:archaeo:dated_to" "archaeo dated to — archaeo relation (archaeo dated to)."
set.add name="rels" id="reldef:archaeo:dated_to"
def "reldef:archaeo:located_in" "archaeo located in — archaeo relation (archaeo located in)."
set.add name="rels" id="reldef:archaeo:located_in"
def "reldef:crafthist:from_country" "crafthist from country — crafthist relation (crafthist from country)."
set.add name="rels" id="reldef:crafthist:from_country"
def "reldef:crafthist:of_period" "crafthist of period — crafthist relation (crafthist of period)."
set.add name="rels" id="reldef:crafthist:of_period"
def "reldef:epic:era_of" "epic era of — epic relation (epic era of)."
set.add name="rels" id="reldef:epic:era_of"
def "reldef:epic:from_culture" "epic from culture — epic relation (epic from culture)."
set.add name="rels" id="reldef:epic:from_culture"
def "reldef:ethnicity:belongs_to" "ethnicity belongs to — ethnicity relation (ethnicity belongs to)."
set.add name="rels" id="reldef:ethnicity:belongs_to"
def "reldef:ethnicity:descends_from" "ethnicity descends from — ethnicity relation (ethnicity descends from)."
set.add name="rels" id="reldef:ethnicity:descends_from"
def "reldef:ethnicity:homeland" "ethnicity homeland — ethnicity relation (ethnicity homeland)."
set.add name="rels" id="reldef:ethnicity:homeland"
def "reldef:ethnicity:speaks" "ethnicity speaks — ethnicity relation (ethnicity speaks)."
set.add name="rels" id="reldef:ethnicity:speaks"
def "reldef:fact:area" "fact area — fact relation (fact area)."
set.add name="rels" id="reldef:fact:area"
def "reldef:fact:gdp" "fact gdp — fact relation (fact gdp)."
set.add name="rels" id="reldef:fact:gdp"
def "reldef:fact:life_expectancy" "fact life expectancy — fact relation (fact life expectancy)."
set.add name="rels" id="reldef:fact:life_expectancy"
def "reldef:fact:population" "fact population — fact relation (fact population)."
set.add name="rels" id="reldef:fact:population"
def "reldef:heritage:in_country" "heritage in country — heritage relation (heritage in country)."
set.add name="rels" id="reldef:heritage:in_country"
def "reldef:heritage:is" "heritage is — heritage relation (heritage is)."
set.add name="rels" id="reldef:heritage:is"
def "reldef:hist:capital_of" "hist capital of — hist relation (hist capital of)."
set.add name="rels" id="reldef:hist:capital_of"
def "reldef:hist:city_in" "hist city in — hist relation (hist city in)."
set.add name="rels" id="reldef:hist:city_in"
def "reldef:hist:cultural_heir" "hist cultural heir — hist relation (hist cultural heir)."
set.add name="rels" id="reldef:hist:cultural_heir"
def "reldef:landmark:in_country" "landmark in country — landmark relation (landmark in country)."
set.add name="rels" id="reldef:landmark:in_country"
def "reldef:landmark:is" "landmark is — landmark relation (landmark is)."
set.add name="rels" id="reldef:landmark:is"
def "reldef:legalhist:dates_from" "legalhist dates from — legalhist relation (legalhist dates from)."
set.add name="rels" id="reldef:legalhist:dates_from"
def "reldef:lit:of_period" "lit of period — lit relation (lit of period)."
set.add name="rels" id="reldef:lit:of_period"
def "reldef:music:of_period" "music of period — music relation (music of period)."
set.add name="rels" id="reldef:music:of_period"
def "reldef:perf:from_country" "perf from country — perf relation (perf from country)."
set.add name="rels" id="reldef:perf:from_country"
def "reldef:perf:of_period" "perf of period — perf relation (perf of period)."
set.add name="rels" id="reldef:perf:of_period"
def "reldef:product:from" "product from — product relation (product from)."
set.add name="rels" id="reldef:product:from"
def "reldef:product:is_a" "product is a — product relation (product is a)."
set.add name="rels" id="reldef:product:is_a"
def "reldef:religion:founded_by" "religion founded by — religion relation (religion founded by)."
set.add name="rels" id="reldef:religion:founded_by"
def "reldef:religion:located_in" "religion located in — religion relation (religion located in)."
set.add name="rels" id="reldef:religion:located_in"
