# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.


# ── namespaces introduced by this pack ──
def "nsdef:adc" "adc — the adc namespace."
set.add name="namespaces" id="nsdef:adc"
def "nsdef:ar" "ar — the ar namespace."
set.add name="namespaces" id="nsdef:ar"
def "nsdef:atr" "atr — the atr namespace."
set.add name="namespaces" id="nsdef:atr"
def "nsdef:ats" "ats — the ats namespace."
set.add name="namespaces" id="nsdef:ats"
def "nsdef:auto" "auto — the auto namespace."
set.add name="namespaces" id="nsdef:auto"
def "nsdef:bcyb" "bcyb — the bcyb namespace."
set.add name="namespaces" id="nsdef:bcyb"
def "nsdef:be" "be — the be namespace."
set.add name="namespaces" id="nsdef:be"
def "nsdef:cloud" "cloud — the cloud namespace."
set.add name="namespaces" id="nsdef:cloud"
def "nsdef:comp" "comp — the comp namespace."
set.add name="namespaces" id="nsdef:comp"
def "nsdef:crypto" "crypto — the crypto namespace."
set.add name="namespaces" id="nsdef:crypto"
def "nsdef:cyber" "cyber — the cyber namespace."
set.add name="namespaces" id="nsdef:cyber"
def "nsdef:datamig" "datamig — the datamig namespace."
set.add name="namespaces" id="nsdef:datamig"
def "nsdef:dl" "dl — the dl namespace."
set.add name="namespaces" id="nsdef:dl"
def "nsdef:dr" "dr — the dr namespace."
set.add name="namespaces" id="nsdef:dr"
def "nsdef:gh" "gh — the gh namespace."
set.add name="namespaces" id="nsdef:gh"
def "nsdef:ha" "ha — the ha namespace."
set.add name="namespaces" id="nsdef:ha"
def "nsdef:iotc" "iotc — the iotc namespace."
set.add name="namespaces" id="nsdef:iotc"
def "nsdef:irob" "irob — the irob namespace."
set.add name="namespaces" id="nsdef:irob"
def "nsdef:journey" "journey — the journey namespace."
set.add name="namespaces" id="nsdef:journey"
def "nsdef:maas" "maas — the maas namespace."
set.add name="namespaces" id="nsdef:maas"
def "nsdef:mas" "mas — the mas namespace."
set.add name="namespaces" id="nsdef:mas"
def "nsdef:mcom" "mcom — the mcom namespace."
set.add name="namespaces" id="nsdef:mcom"
def "nsdef:mfos" "mfos — the mfos namespace."
set.add name="namespaces" id="nsdef:mfos"
def "nsdef:modernization" "modernization — the modernization namespace."
set.add name="namespaces" id="nsdef:modernization"
def "nsdef:off" "off — the off namespace."
set.add name="namespaces" id="nsdef:off"
def "nsdef:oslineage" "oslineage — the oslineage namespace."
set.add name="namespaces" id="nsdef:oslineage"
def "nsdef:process" "process — the process namespace."
set.add name="namespaces" id="nsdef:process"
def "nsdef:prop" "prop — the prop namespace."
set.add name="namespaces" id="nsdef:prop"
def "nsdef:rtos" "rtos — the rtos namespace."
set.add name="namespaces" id="nsdef:rtos"
def "nsdef:satcom" "satcom — the satcom namespace."
set.add name="namespaces" id="nsdef:satcom"
def "nsdef:sg" "sg — the sg namespace."
set.add name="namespaces" id="nsdef:sg"
def "nsdef:spaceproto" "spaceproto — the spaceproto namespace."
set.add name="namespaces" id="nsdef:spaceproto"
def "nsdef:sr" "sr — the sr namespace."
set.add name="namespaces" id="nsdef:sr"
def "nsdef:subcom" "subcom — the subcom namespace."
set.add name="namespaces" id="nsdef:subcom"
def "nsdef:sw" "sw — the sw namespace."
set.add name="namespaces" id="nsdef:sw"
def "nsdef:udr" "udr — the udr namespace."
set.add name="namespaces" id="nsdef:udr"
def "nsdef:uinf" "uinf — the uinf namespace."
set.add name="namespaces" id="nsdef:uinf"
def "nsdef:virt" "virt — the virt namespace."
set.add name="namespaces" id="nsdef:virt"

# ── relations introduced by this pack ──
def "reldef:@abstracts" "abstracts — 'abstracts' relation asserted between two atoms."
set.add name="rels" id="reldef:@abstracts"
def "reldef:@advanced_form_of" "advanced form of — 'advanced form of' relation asserted between two atoms."
set.add name="rels" id="reldef:@advanced_form_of"
def "reldef:@affects" "affects — 'affects' relation asserted between two atoms."
set.add name="rels" id="reldef:@affects"
def "reldef:@aligned_with" "aligned with — 'aligned with' relation asserted between two atoms."
set.add name="rels" id="reldef:@aligned_with"
def "reldef:@allocates" "allocates — 'allocates' relation asserted between two atoms."
set.add name="rels" id="reldef:@allocates"
def "reldef:@analyzed_with" "analyzed with — 'analyzed with' relation asserted between two atoms."
set.add name="rels" id="reldef:@analyzed_with"
def "reldef:@ancestor_of" "ancestor of — 'ancestor of' relation asserted between two atoms."
set.add name="rels" id="reldef:@ancestor_of"
def "reldef:@applied_to" "applied to — 'applied to' relation asserted between two atoms."
set.add name="rels" id="reldef:@applied_to"
def "reldef:@applies_to" "applies to — 'applies to' relation asserted between two atoms."
set.add name="rels" id="reldef:@applies_to"
def "reldef:@base_of" "base of — 'base of' relation asserted between two atoms."
set.add name="rels" id="reldef:@base_of"
def "reldef:@bridges" "bridges — 'bridges' relation asserted between two atoms."
set.add name="rels" id="reldef:@bridges"
def "reldef:@builds_on" "builds on — 'builds on' relation asserted between two atoms."
set.add name="rels" id="reldef:@builds_on"
def "reldef:@can_cause" "can cause — 'can cause' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_cause"
def "reldef:@can_implement" "can implement — 'can implement' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_implement"
def "reldef:@can_prevent" "can prevent — 'can prevent' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_prevent"
def "reldef:@can_run_over" "can run over — 'can run over' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_run_over"
def "reldef:@carried_by" "carried by — 'carried by' relation asserted between two atoms."
set.add name="rels" id="reldef:@carried_by"
def "reldef:@changes" "changes — 'changes' relation asserted between two atoms."
set.add name="rels" id="reldef:@changes"
def "reldef:@characterizes" "characterizes — 'characterizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@characterizes"
def "reldef:@charges" "charges — 'charges' relation asserted between two atoms."
set.add name="rels" id="reldef:@charges"
def "reldef:@coexists_with" "coexists with — 'coexists with' relation asserted between two atoms."
set.add name="rels" id="reldef:@coexists_with"
def "reldef:@combined_in" "combined in — 'combined in' relation asserted between two atoms."
set.add name="rels" id="reldef:@combined_in"
def "reldef:@complements" "complements — 'complements' relation asserted between two atoms."
set.add name="rels" id="reldef:@complements"
def "reldef:@configures" "configures — 'configures' relation asserted between two atoms."
set.add name="rels" id="reldef:@configures"
def "reldef:@connects" "connects — 'connects' relation asserted between two atoms."
set.add name="rels" id="reldef:@connects"
def "reldef:@constrained_by" "constrained by — 'constrained by' relation asserted between two atoms."
set.add name="rels" id="reldef:@constrained_by"
def "reldef:@constrains" "constrains — 'constrains' relation asserted between two atoms."
set.add name="rels" id="reldef:@constrains"
def "reldef:@controlled_by" "controlled by — 'controlled by' relation asserted between two atoms."
set.add name="rels" id="reldef:@controlled_by"
def "reldef:@controls" "controls — 'controls' relation asserted between two atoms."
set.add name="rels" id="reldef:@controls"
def "reldef:@cooperates_with" "cooperates with — 'cooperates with' relation asserted between two atoms."
set.add name="rels" id="reldef:@cooperates_with"
def "reldef:@core_method" "core method — 'core method' relation asserted between two atoms."
set.add name="rels" id="reldef:@core_method"
def "reldef:@core_protocol" "core protocol — 'core protocol' relation asserted between two atoms."
set.add name="rels" id="reldef:@core_protocol"
def "reldef:@defined_by" "defined by — 'defined by' relation asserted between two atoms."
set.add name="rels" id="reldef:@defined_by"
def "reldef:@defines" "defines — 'defines' relation asserted between two atoms."
set.add name="rels" id="reldef:@defines"
def "reldef:@describes" "describes — 'describes' relation asserted between two atoms."
set.add name="rels" id="reldef:@describes"
def "reldef:@detects" "detects — 'detects' relation asserted between two atoms."
set.add name="rels" id="reldef:@detects"
def "reldef:@drives_demand_for" "drives demand for — 'drives demand for' relation asserted between two atoms."
set.add name="rels" id="reldef:@drives_demand_for"
def "reldef:@enables_long_distance" "enables long distance — 'enables long distance' relation asserted between two atoms."
set.add name="rels" id="reldef:@enables_long_distance"
def "reldef:@enables_low_downtime" "enables low downtime — 'enables low downtime' relation asserted between two atoms."
set.add name="rels" id="reldef:@enables_low_downtime"
def "reldef:@evaluated_by" "evaluated by — 'evaluated by' relation asserted between two atoms."
set.add name="rels" id="reldef:@evaluated_by"
def "reldef:@evolves_from" "evolves from — 'evolves from' relation asserted between two atoms."
set.add name="rels" id="reldef:@evolves_from"
def "reldef:@evolves_to" "evolves to — 'evolves to' relation asserted between two atoms."
set.add name="rels" id="reldef:@evolves_to"
def "reldef:@expands" "expands — 'expands' relation asserted between two atoms."
set.add name="rels" id="reldef:@expands"
def "reldef:@feeds" "feeds — 'feeds' relation asserted between two atoms."
set.add name="rels" id="reldef:@feeds"
def "reldef:@finalizes" "finalizes — 'finalizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@finalizes"
def "reldef:@future_supports" "future supports — 'future supports' relation asserted between two atoms."
set.add name="rels" id="reldef:@future_supports"
def "reldef:@generates" "generates — 'generates' relation asserted between two atoms."
set.add name="rels" id="reldef:@generates"
def "reldef:@generates_efficiently" "generates efficiently — 'generates efficiently' relation asserted between two atoms."
set.add name="rels" id="reldef:@generates_efficiently"
def "reldef:@hosts" "hosts — 'hosts' relation asserted between two atoms."
set.add name="rels" id="reldef:@hosts"
def "reldef:@implements" "implements — 'implements' relation asserted between two atoms."
set.add name="rels" id="reldef:@implements"
def "reldef:@improves" "improves — 'improves' relation asserted between two atoms."
set.add name="rels" id="reldef:@improves"
def "reldef:@influences_conceptually" "influences conceptually — 'influences conceptually' relation asserted between two atoms."
set.add name="rels" id="reldef:@influences_conceptually"
def "reldef:@informs" "informs — 'informs' relation asserted between two atoms."
set.add name="rels" id="reldef:@informs"
def "reldef:@integrates_with" "integrates with — 'integrates with' relation asserted between two atoms."
set.add name="rels" id="reldef:@integrates_with"
def "reldef:@is_a_type_of" "is a type of — 'is a type of' relation asserted between two atoms."
set.add name="rels" id="reldef:@is_a_type_of"
def "reldef:@limits_distance" "limits distance — 'limits distance' relation asserted between two atoms."
set.add name="rels" id="reldef:@limits_distance"
def "reldef:@manages" "manages — 'manages' relation asserted between two atoms."
set.add name="rels" id="reldef:@manages"
def "reldef:@may_cause" "may cause — 'may cause' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_cause"
def "reldef:@may_create" "may create — 'may create' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_create"
def "reldef:@may_define" "may define — 'may define' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_define"
def "reldef:@may_flow_through" "may flow through — 'may flow through' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_flow_through"
def "reldef:@may_implement" "may implement — 'may implement' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_implement"
def "reldef:@may_increase" "may increase — 'may increase' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_increase"
def "reldef:@may_result_from" "may result from — 'may result from' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_result_from"
def "reldef:@mechanizes" "mechanizes — 'mechanizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@mechanizes"
def "reldef:@minimizes" "minimizes — 'minimizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@minimizes"
def "reldef:@mitigated_by" "mitigated by — 'mitigated by' relation asserted between two atoms."
set.add name="rels" id="reldef:@mitigated_by"
def "reldef:@mitigates" "mitigates — 'mitigates' relation asserted between two atoms."
set.add name="rels" id="reldef:@mitigates"
def "reldef:@modeled_by" "modeled by — 'modeled by' relation asserted between two atoms."
set.add name="rels" id="reldef:@modeled_by"
def "reldef:@models" "models — 'models' relation asserted between two atoms."
set.add name="rels" id="reldef:@models"
def "reldef:@monitored_by" "monitored by — 'monitored by' relation asserted between two atoms."
set.add name="rels" id="reldef:@monitored_by"
def "reldef:@monitors" "monitors — 'monitors' relation asserted between two atoms."
set.add name="rels" id="reldef:@monitors"
def "reldef:@often_expressed_in" "often expressed in — 'often expressed in' relation asserted between two atoms."
set.add name="rels" id="reldef:@often_expressed_in"
def "reldef:@often_implemented_as" "often implemented as — 'often implemented as' relation asserted between two atoms."
set.add name="rels" id="reldef:@often_implemented_as"
def "reldef:@operates_on" "operates on — 'operates on' relation asserted between two atoms."
set.add name="rels" id="reldef:@operates_on"
def "reldef:@optimized_by" "optimized by — 'optimized by' relation asserted between two atoms."
set.add name="rels" id="reldef:@optimized_by"
def "reldef:@pairs_with" "pairs with — 'pairs with' relation asserted between two atoms."
set.add name="rels" id="reldef:@pairs_with"
def "reldef:@part_of" "part of — 'part of' relation asserted between two atoms."
set.add name="rels" id="reldef:@part_of"
def "reldef:@participates_in" "participates in — 'participates in' relation asserted between two atoms."
set.add name="rels" id="reldef:@participates_in"
def "reldef:@physically_implements" "physically implements — 'physically implements' relation asserted between two atoms."
set.add name="rels" id="reldef:@physically_implements"
def "reldef:@protects" "protects — 'protects' relation asserted between two atoms."
set.add name="rels" id="reldef:@protects"
def "reldef:@provides_continuous" "provides continuous — 'provides continuous' relation asserted between two atoms."
set.add name="rels" id="reldef:@provides_continuous"
def "reldef:@reduced_by" "reduced by — 'reduced by' relation asserted between two atoms."
set.add name="rels" id="reldef:@reduced_by"
def "reldef:@reduces_load" "reduces load — 'reduces load' relation asserted between two atoms."
set.add name="rels" id="reldef:@reduces_load"
def "reldef:@relies_on_advanced" "relies on advanced — 'relies on advanced' relation asserted between two atoms."
set.add name="rels" id="reldef:@relies_on_advanced"
def "reldef:@replaces" "replaces — 'replaces' relation asserted between two atoms."
set.add name="rels" id="reldef:@replaces"
def "reldef:@required_for" "required for — 'required for' relation asserted between two atoms."
set.add name="rels" id="reldef:@required_for"
def "reldef:@reverts" "reverts — 'reverts' relation asserted between two atoms."
set.add name="rels" id="reldef:@reverts"
def "reldef:@runs_on" "runs on — 'runs on' relation asserted between two atoms."
set.add name="rels" id="reldef:@runs_on"
def "reldef:@runs_over" "runs over — 'runs over' relation asserted between two atoms."
set.add name="rels" id="reldef:@runs_over"
def "reldef:@runs_within" "runs within — 'runs within' relation asserted between two atoms."
set.add name="rels" id="reldef:@runs_within"
def "reldef:@secures" "secures — 'secures' relation asserted between two atoms."
set.add name="rels" id="reldef:@secures"
def "reldef:@signals" "signals — 'signals' relation asserted between two atoms."
set.add name="rels" id="reldef:@signals"
def "reldef:@similar_to" "similar to — 'similar to' relation asserted between two atoms."
set.add name="rels" id="reldef:@similar_to"
def "reldef:@specializes" "specializes — 'specializes' relation asserted between two atoms."
set.add name="rels" id="reldef:@specializes"
def "reldef:@specifies" "specifies — 'specifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@specifies"
def "reldef:@stabilizes" "stabilizes — 'stabilizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@stabilizes"
def "reldef:@stores" "stores — 'stores' relation asserted between two atoms."
set.add name="rels" id="reldef:@stores"
def "reldef:@subdivides" "subdivides — 'subdivides' relation asserted between two atoms."
set.add name="rels" id="reldef:@subdivides"
def "reldef:@subset_of" "subset of — 'subset of' relation asserted between two atoms."
set.add name="rels" id="reldef:@subset_of"
def "reldef:@supported_by" "supported by — 'supported by' relation asserted between two atoms."
set.add name="rels" id="reldef:@supported_by"
def "reldef:@typically_implemented_as" "typically implemented as — 'typically implemented as' relation asserted between two atoms."
set.add name="rels" id="reldef:@typically_implemented_as"
def "reldef:@unifies" "unifies — 'unifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@unifies"
def "reldef:@used_by" "used by — 'used by' relation asserted between two atoms."
set.add name="rels" id="reldef:@used_by"
def "reldef:@used_in" "used in — 'used in' relation asserted between two atoms."
set.add name="rels" id="reldef:@used_in"
def "reldef:@used_with" "used with — 'used with' relation asserted between two atoms."
set.add name="rels" id="reldef:@used_with"
def "reldef:@utilized_in" "utilized in — 'utilized in' relation asserted between two atoms."
set.add name="rels" id="reldef:@utilized_in"
def "reldef:@verifies" "verifies — 'verifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@verifies"
