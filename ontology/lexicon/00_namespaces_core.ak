# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.

def "nsdef:acad" "acad — the acad namespace."
set.add name="namespaces" id="nsdef:acad"
def "nsdef:acad_admin" "acad_admin — the acad admin namespace."
set.add name="namespaces" id="nsdef:acad_admin"
def "nsdef:accessory" "accessory — the accessory namespace."
set.add name="namespaces" id="nsdef:accessory"
def "nsdef:accessory_type" "accessory_type — the accessory type namespace."
set.add name="namespaces" id="nsdef:accessory_type"
def "nsdef:acct" "acct — the acct namespace."
set.add name="namespaces" id="nsdef:acct"
def "nsdef:activity" "activity — the activity namespace."
set.add name="namespaces" id="nsdef:activity"
def "nsdef:admin" "admin — administrative divisions and scale."
set.add name="namespaces" id="nsdef:admin"
def "nsdef:admin_scale" "admin_scale — the admin scale namespace."
set.add name="namespaces" id="nsdef:admin_scale"
def "nsdef:adv" "adv — the adv namespace."
set.add name="namespaces" id="nsdef:adv"
def "nsdef:ag" "ag — the ag namespace."
set.add name="namespaces" id="nsdef:ag"
def "nsdef:aging" "aging — the aging namespace."
set.add name="namespaces" id="nsdef:aging"
def "nsdef:agri" "agri — the agri namespace."
set.add name="namespaces" id="nsdef:agri"
def "nsdef:agron" "agron — the agron namespace."
set.add name="namespaces" id="nsdef:agron"
def "nsdef:ai" "ai — the ai namespace."
set.add name="namespaces" id="nsdef:ai"
def "nsdef:allergen" "allergen — allergens."
set.add name="namespaces" id="nsdef:allergen"
def "nsdef:anth" "anth — the anth namespace."
set.add name="namespaces" id="nsdef:anth"
def "nsdef:anthro" "anthro — cultural anthropology."
set.add name="namespaces" id="nsdef:anthro"
def "nsdef:appliance" "appliance — the appliance namespace."
set.add name="namespaces" id="nsdef:appliance"
def "nsdef:arch" "arch — architecture and architectural styles."
set.add name="namespaces" id="nsdef:arch"
def "nsdef:archaeo" "archaeo — archaeology."
set.add name="namespaces" id="nsdef:archaeo"
def "nsdef:art" "art — visual art movements and works."
set.add name="namespaces" id="nsdef:art"
def "nsdef:artsci" "artsci — the artsci namespace."
set.add name="namespaces" id="nsdef:artsci"
def "nsdef:asc" "asc — the asc namespace."
set.add name="namespaces" id="nsdef:asc"
def "nsdef:astro" "astro — astronomy."
set.add name="namespaces" id="nsdef:astro"
def "nsdef:astrophys" "astrophys — astrophysics."
set.add name="namespaces" id="nsdef:astrophys"
def "nsdef:ayurveda" "ayurveda — Ayurvedic properties."
set.add name="namespaces" id="nsdef:ayurveda"
def "nsdef:beer" "beer — beers and styles."
set.add name="namespaces" id="nsdef:beer"
def "nsdef:beverage" "beverage — drinks and beverages."
set.add name="namespaces" id="nsdef:beverage"
def "nsdef:bib" "bib — the bib namespace."
set.add name="namespaces" id="nsdef:bib"
def "nsdef:bio" "bio — biological classification and properties."
set.add name="namespaces" id="nsdef:bio"
def "nsdef:bio_sci" "bio_sci — biology, general."
set.add name="namespaces" id="nsdef:bio_sci"
def "nsdef:biz" "biz — the biz namespace."
set.add name="namespaces" id="nsdef:biz"
def "nsdef:bme" "bme — the bme namespace."
set.add name="namespaces" id="nsdef:bme"
def "nsdef:broadcast" "broadcast — the broadcast namespace."
set.add name="namespaces" id="nsdef:broadcast"
def "nsdef:broadcast_type" "broadcast_type — the broadcast type namespace."
set.add name="namespaces" id="nsdef:broadcast_type"
def "nsdef:c" "c — the c namespace."
set.add name="namespaces" id="nsdef:c"
def "nsdef:calc" "calc — computed / curated associations and derived values."
set.add name="namespaces" id="nsdef:calc"
def "nsdef:care" "care — the care namespace."
set.add name="namespaces" id="nsdef:care"
def "nsdef:cartog" "cartog — the cartog namespace."
set.add name="namespaces" id="nsdef:cartog"
def "nsdef:cc" "cc — the cc namespace."
set.add name="namespaces" id="nsdef:cc"
def "nsdef:ce" "ce — the ce namespace."
set.add name="namespaces" id="nsdef:ce"
def "nsdef:cheese" "cheese — cheeses and their origins."
set.add name="namespaces" id="nsdef:cheese"
def "nsdef:chem" "chem — chemistry."
set.add name="namespaces" id="nsdef:chem"
def "nsdef:chemeng" "chemeng — the chemeng namespace."
set.add name="namespaces" id="nsdef:chemeng"
def "nsdef:chocolate" "chocolate — chocolate and cacao."
set.add name="namespaces" id="nsdef:chocolate"
def "nsdef:chrono" "chrono — temporal placement (before / after)."
set.add name="namespaces" id="nsdef:chrono"
def "nsdef:cite" "cite — the cite namespace."
set.add name="namespaces" id="nsdef:cite"
def "nsdef:civ" "civ — the civ namespace."
set.add name="namespaces" id="nsdef:civ"
def "nsdef:civic" "civic — civic institutions and roles."
set.add name="namespaces" id="nsdef:civic"
def "nsdef:ck" "ck — the ck namespace."
set.add name="namespaces" id="nsdef:ck"
def "nsdef:climate" "climate — the climate namespace."
set.add name="namespaces" id="nsdef:climate"
def "nsdef:clothing" "clothing — clothing and dress."
set.add name="namespaces" id="nsdef:clothing"
def "nsdef:clothing_zone" "clothing_zone — the clothing zone namespace."
set.add name="namespaces" id="nsdef:clothing_zone"
def "nsdef:cocktail" "cocktail — cocktails and mixed drinks."
set.add name="namespaces" id="nsdef:cocktail"
def "nsdef:coffee" "coffee — coffees and origins."
set.add name="namespaces" id="nsdef:coffee"
def "nsdef:cog" "cog — the cog namespace."
set.add name="namespaces" id="nsdef:cog"
def "nsdef:collab" "collab — the collab namespace."
set.add name="namespaces" id="nsdef:collab"
def "nsdef:color" "color — colour terms and palette."
set.add name="namespaces" id="nsdef:color"
def "nsdef:concept" "concept — abstract concepts."
set.add name="namespaces" id="nsdef:concept"
def "nsdef:condiment_type" "condiment_type — the condiment type namespace."
set.add name="namespaces" id="nsdef:condiment_type"
def "nsdef:conf" "conf — the conf namespace."
set.add name="namespaces" id="nsdef:conf"
def "nsdef:conflict" "conflict — narrative conflict types."
set.add name="namespaces" id="nsdef:conflict"
def "nsdef:constraint" "constraint — the constraint namespace."
set.add name="namespaces" id="nsdef:constraint"
def "nsdef:cook" "cook — the cook namespace."
set.add name="namespaces" id="nsdef:cook"
def "nsdef:cook_class" "cook_class — the cook class namespace."
set.add name="namespaces" id="nsdef:cook_class"
def "nsdef:cosmetic" "cosmetic — the cosmetic namespace."
set.add name="namespaces" id="nsdef:cosmetic"
def "nsdef:cosmetic_type" "cosmetic_type — the cosmetic type namespace."
set.add name="namespaces" id="nsdef:cosmetic_type"
def "nsdef:counsel" "counsel — the counsel namespace."
set.add name="namespaces" id="nsdef:counsel"
def "nsdef:country" "country — the country namespace."
set.add name="namespaces" id="nsdef:country"
def "nsdef:craft" "craft — the craft namespace."
set.add name="namespaces" id="nsdef:craft"
def "nsdef:crafthist" "crafthist — the crafthist namespace."
set.add name="namespaces" id="nsdef:crafthist"
def "nsdef:crosscut" "crosscut — cross-cutting themes across disciplines."
set.add name="namespaces" id="nsdef:crosscut"
def "nsdef:cs" "cs — the cs namespace."
set.add name="namespaces" id="nsdef:cs"
def "nsdef:ctrl" "ctrl — the ctrl namespace."
set.add name="namespaces" id="nsdef:ctrl"
def "nsdef:cuisine" "cuisine — culinary traditions by region."
set.add name="namespaces" id="nsdef:cuisine"
def "nsdef:culinary" "culinary — culinary techniques, cuts, components and uses."
set.add name="namespaces" id="nsdef:culinary"
def "nsdef:cut_type" "cut_type — the cut type namespace."
set.add name="namespaces" id="nsdef:cut_type"
def "nsdef:cyb" "cyb — the cyb namespace."
set.add name="namespaces" id="nsdef:cyb"
def "nsdef:data" "data — the data namespace."
set.add name="namespaces" id="nsdef:data"
def "nsdef:dc" "dc — the dc namespace."
set.add name="namespaces" id="nsdef:dc"
def "nsdef:decor" "decor — the decor namespace."
set.add name="namespaces" id="nsdef:decor"
def "nsdef:decor_type" "decor_type — the decor type namespace."
set.add name="namespaces" id="nsdef:decor_type"
def "nsdef:deity" "deity — deities and the domains they embody."
set.add name="namespaces" id="nsdef:deity"
def "nsdef:devpsy" "devpsy — the devpsy namespace."
set.add name="namespaces" id="nsdef:devpsy"
def "nsdef:dh" "dh — digital humanities."
set.add name="namespaces" id="nsdef:dh"
def "nsdef:dh_area" "dh_area — the dh area namespace."
set.add name="namespaces" id="nsdef:dh_area"
def "nsdef:diet" "diet — diets and dietary patterns."
set.add name="namespaces" id="nsdef:diet"
def "nsdef:dietflag" "dietflag — dietary classification flags."
set.add name="namespaces" id="nsdef:dietflag"
def "nsdef:dietlaw" "dietlaw — religious and cultural dietary laws."
set.add name="namespaces" id="nsdef:dietlaw"
def "nsdef:digarch" "digarch — the digarch namespace."
set.add name="namespaces" id="nsdef:digarch"
def "nsdef:diplo" "diplo — the diplo namespace."
set.add name="namespaces" id="nsdef:diplo"
def "nsdef:dish" "dish — prepared dishes."
set.add name="namespaces" id="nsdef:dish"
def "nsdef:dna" "dna — innate sensory qualities keyed to a food's identity."
set.add name="namespaces" id="nsdef:dna"
def "nsdef:dna:smell" "dna:smell — innate olfactory qualities."
set.add name="namespaces" id="nsdef:dna:smell"
def "nsdef:dna:sound" "dna:sound — innate sound qualities."
set.add name="namespaces" id="nsdef:dna:sound"
def "nsdef:dna:taste" "dna:taste — innate gustatory qualities (sweet, sour, bitter, salty, umami)."
set.add name="namespaces" id="nsdef:dna:taste"
def "nsdef:dna:texture" "dna:texture — innate textural qualities."
set.add name="namespaces" id="nsdef:dna:texture"
def "nsdef:earthsci" "earthsci — the earthsci namespace."
set.add name="namespaces" id="nsdef:earthsci"
def "nsdef:ecol" "ecol — the ecol namespace."
set.add name="namespaces" id="nsdef:ecol"
def "nsdef:econ" "econ — economics."
set.add name="namespaces" id="nsdef:econ"
def "nsdef:econ_field" "econ_field — the econ field namespace."
set.add name="namespaces" id="nsdef:econ_field"
def "nsdef:edupsy" "edupsy — the edupsy namespace."
set.add name="namespaces" id="nsdef:edupsy"
def "nsdef:eleceng" "eleceng — the eleceng namespace."
set.add name="namespaces" id="nsdef:eleceng"
def "nsdef:electron" "electron — the electron namespace."
set.add name="namespaces" id="nsdef:electron"
def "nsdef:emo" "emo — Plutchik emotion axes (joy, trust, fear, ...)."
set.add name="namespaces" id="nsdef:emo"
def "nsdef:energy" "energy — the energy namespace."
set.add name="namespaces" id="nsdef:energy"
def "nsdef:eng" "eng — engineering, general."
set.add name="namespaces" id="nsdef:eng"
def "nsdef:enveng" "enveng — the enveng namespace."
set.add name="namespaces" id="nsdef:enveng"
def "nsdef:epic" "epic — epics and foundational texts."
set.add name="namespaces" id="nsdef:epic"
def "nsdef:epig" "epig — the epig namespace."
set.add name="namespaces" id="nsdef:epig"
def "nsdef:era" "era — historical eras."
set.add name="namespaces" id="nsdef:era"
def "nsdef:ethnicity" "ethnicity — peoples and ethnic groups."
set.add name="namespaces" id="nsdef:ethnicity"
def "nsdef:event" "event — the event namespace."
set.add name="namespaces" id="nsdef:event"
def "nsdef:event_type" "event_type — the event type namespace."
set.add name="namespaces" id="nsdef:event_type"
def "nsdef:ext" "ext — external routes and resource links (provenance atoms)."
set.add name="namespaces" id="nsdef:ext"
def "nsdef:fat_source" "fat_source — the fat source namespace."
set.add name="namespaces" id="nsdef:fat_source"
def "nsdef:ferment_type" "ferment_type — the ferment type namespace."
set.add name="namespaces" id="nsdef:ferment_type"
def "nsdef:fin" "fin — the fin namespace."
set.add name="namespaces" id="nsdef:fin"
def "nsdef:flavor_base" "flavor_base — the flavor base namespace."
set.add name="namespaces" id="nsdef:flavor_base"
def "nsdef:flesh_type" "flesh_type — the flesh type namespace."
set.add name="namespaces" id="nsdef:flesh_type"
def "nsdef:fnty" "fnty — the fnty namespace."
set.add name="namespaces" id="nsdef:fnty"
def "nsdef:fodmap_level" "fodmap_level — the fodmap level namespace."
set.add name="namespaces" id="nsdef:fodmap_level"
def "nsdef:folk" "folk — folklore."
set.add name="namespaces" id="nsdef:folk"
def "nsdef:food" "food — the food domain (ingredients, dishes, cuisines)."
set.add name="namespaces" id="nsdef:food"
def "nsdef:foodprecis" "foodprecis — the foodprecis namespace."
set.add name="namespaces" id="nsdef:foodprecis"
def "nsdef:foodsafe" "foodsafe — the foodsafe namespace."
set.add name="namespaces" id="nsdef:foodsafe"
def "nsdef:foodsci" "foodsci — the foodsci namespace."
set.add name="namespaces" id="nsdef:foodsci"
def "nsdef:foodsec" "foodsec — the foodsec namespace."
set.add name="namespaces" id="nsdef:foodsec"
def "nsdef:foodstud" "foodstud — the foodstud namespace."
set.add name="namespaces" id="nsdef:foodstud"
def "nsdef:foodsustain" "foodsustain — the foodsustain namespace."
set.add name="namespaces" id="nsdef:foodsustain"
def "nsdef:formality" "formality — the formality namespace."
set.add name="namespaces" id="nsdef:formality"
def "nsdef:fp" "fp — the fp namespace."
set.add name="namespaces" id="nsdef:fp"
def "nsdef:free_from" "free_from — the free from namespace."
set.add name="namespaces" id="nsdef:free_from"
def "nsdef:fruit_type" "fruit_type — the fruit type namespace."
set.add name="namespaces" id="nsdef:fruit_type"
def "nsdef:furniture" "furniture — the furniture namespace."
set.add name="namespaces" id="nsdef:furniture"
def "nsdef:futureag" "futureag — the futureag namespace."
set.add name="namespaces" id="nsdef:futureag"
def "nsdef:futurefood" "futurefood — the futurefood namespace."
set.add name="namespaces" id="nsdef:futurefood"
def "nsdef:fx" "fx — the fx namespace."
set.add name="namespaces" id="nsdef:fx"
def "nsdef:garnish" "garnish — cocktail and dish garnishes."
set.add name="namespaces" id="nsdef:garnish"
def "nsdef:geo" "geo — geography: places, borders, countries, languages."
set.add name="namespaces" id="nsdef:geo"
def "nsdef:geog" "geog — the geog namespace."
set.add name="namespaces" id="nsdef:geog"
def "nsdef:gi_level" "gi_level — the gi level namespace."
set.add name="namespaces" id="nsdef:gi_level"
def "nsdef:gov" "gov — government functions and services."
set.add name="namespaces" id="nsdef:gov"
def "nsdef:grain_type" "grain_type — the grain type namespace."
set.add name="namespaces" id="nsdef:grain_type"
def "nsdef:group" "group — the group namespace."
set.add name="namespaces" id="nsdef:group"
def "nsdef:group_type" "group_type — the group type namespace."
set.add name="namespaces" id="nsdef:group_type"
def "nsdef:habitat" "habitat — the habitat namespace."
set.add name="namespaces" id="nsdef:habitat"
def "nsdef:haccp" "haccp — the haccp namespace."
set.add name="namespaces" id="nsdef:haccp"
def "nsdef:heat_level" "heat_level — the heat level namespace."
set.add name="namespaces" id="nsdef:heat_level"
def "nsdef:heat_source" "heat_source — the heat source namespace."
set.add name="namespaces" id="nsdef:heat_source"
def "nsdef:hemisphere" "hemisphere — the hemisphere namespace."
set.add name="namespaces" id="nsdef:hemisphere"
def "nsdef:herb_type" "herb_type — the herb type namespace."
set.add name="namespaces" id="nsdef:herb_type"
def "nsdef:heritage" "heritage — heritage sites and traditions."
set.add name="namespaces" id="nsdef:heritage"
def "nsdef:hist" "hist — historical polities, periods and events."
set.add name="namespaces" id="nsdef:hist"
def "nsdef:home" "home — the everyday home: rooms, zones, household items."
set.add name="namespaces" id="nsdef:home"
def "nsdef:home_type" "home_type — the home type namespace."
set.add name="namespaces" id="nsdef:home_type"
def "nsdef:home_zone" "home_zone — the home zone namespace."
set.add name="namespaces" id="nsdef:home_zone"
def "nsdef:husbandry" "husbandry — the husbandry namespace."
set.add name="namespaces" id="nsdef:husbandry"
def "nsdef:idea" "idea — the idea namespace."
set.add name="namespaces" id="nsdef:idea"
def "nsdef:infra" "infra — the infra namespace."
set.add name="namespaces" id="nsdef:infra"
def "nsdef:ingred" "ingred — food ingredients."
set.add name="namespaces" id="nsdef:ingred"
def "nsdef:instr" "instr — musical instruments."
set.add name="namespaces" id="nsdef:instr"
def "nsdef:insur" "insur — the insur namespace."
set.add name="namespaces" id="nsdef:insur"
def "nsdef:intl" "intl — the intl namespace."
set.add name="namespaces" id="nsdef:intl"
def "nsdef:invest" "invest — the invest namespace."
set.add name="namespaces" id="nsdef:invest"
def "nsdef:iot" "iot — the iot namespace."
set.add name="namespaces" id="nsdef:iot"
def "nsdef:kin" "kin — kinship roles and relations."
set.add name="namespaces" id="nsdef:kin"
def "nsdef:knowledge" "knowledge — the cross-disciplinary knowledge map."
set.add name="namespaces" id="nsdef:knowledge"
def "nsdef:landmark" "landmark — landmarks and monuments."
set.add name="namespaces" id="nsdef:landmark"
def "nsdef:lang" "lang — the lang namespace."
set.add name="namespaces" id="nsdef:lang"
def "nsdef:law" "law — legal traditions and codes."
set.add name="namespaces" id="nsdef:law"
def "nsdef:legume_type" "legume_type — the legume type namespace."
set.add name="namespaces" id="nsdef:legume_type"
def "nsdef:lib" "lib — the lib namespace."
set.add name="namespaces" id="nsdef:lib"
def "nsdef:life" "life — the life namespace."
set.add name="namespaces" id="nsdef:life"
def "nsdef:lineage" "lineage — the lineage namespace."
set.add name="namespaces" id="nsdef:lineage"
def "nsdef:ling" "ling — linguistics."
set.add name="namespaces" id="nsdef:ling"
def "nsdef:liqueur" "liqueur — liqueurs."
set.add name="namespaces" id="nsdef:liqueur"
def "nsdef:lit" "lit — the lit namespace."
set.add name="namespaces" id="nsdef:lit"
def "nsdef:log" "log — the log namespace."
set.add name="namespaces" id="nsdef:log"
def "nsdef:map" "map — the map namespace."
set.add name="namespaces" id="nsdef:map"
def "nsdef:mat" "mat — the mat namespace."
set.add name="namespaces" id="nsdef:mat"
def "nsdef:math" "math — mathematics."
set.add name="namespaces" id="nsdef:math"
def "nsdef:matsci" "matsci — the matsci namespace."
set.add name="namespaces" id="nsdef:matsci"
def "nsdef:mecha" "mecha — the mecha namespace."
set.add name="namespaces" id="nsdef:mecha"
def "nsdef:mecheng" "mecheng — the mecheng namespace."
set.add name="namespaces" id="nsdef:mecheng"
def "nsdef:med" "med — medicine."
set.add name="namespaces" id="nsdef:med"
def "nsdef:media" "media — media resources (images, video) and what they depict."
set.add name="namespaces" id="nsdef:media"
def "nsdef:meta" "meta — the meta namespace."
set.add name="namespaces" id="nsdef:meta"
def "nsdef:metastd" "metastd — the metastd namespace."
set.add name="namespaces" id="nsdef:metastd"
def "nsdef:method" "method — the method namespace."
set.add name="namespaces" id="nsdef:method"
def "nsdef:mfg" "mfg — the mfg namespace."
set.add name="namespaces" id="nsdef:mfg"
def "nsdef:mgmt" "mgmt — the mgmt namespace."
set.add name="namespaces" id="nsdef:mgmt"
def "nsdef:mkt" "mkt — the mkt namespace."
set.add name="namespaces" id="nsdef:mkt"
def "nsdef:mn" "mn — the mn namespace."
set.add name="namespaces" id="nsdef:mn"
def "nsdef:month" "month — the month namespace."
set.add name="namespaces" id="nsdef:month"
def "nsdef:ms" "ms — the ms namespace."
set.add name="namespaces" id="nsdef:ms"
def "nsdef:mus" "mus — the mus namespace."
set.add name="namespaces" id="nsdef:mus"
def "nsdef:music" "music — music, periods and instruments."
set.add name="namespaces" id="nsdef:music"
def "nsdef:myth" "myth — myths and mythological figures."
set.add name="namespaces" id="nsdef:myth"
def "nsdef:nar" "nar — narrative craft (structure, character, conflict, device)."
set.add name="namespaces" id="nsdef:nar"
def "nsdef:net" "net — the net namespace."
set.add name="namespaces" id="nsdef:net"
def "nsdef:neuro" "neuro — neuroscience."
set.add name="namespaces" id="nsdef:neuro"
def "nsdef:nsdef" "nsdef — namespace-definition atoms; nsdef:<ns> holds a namespace's description."
set.add name="namespaces" id="nsdef:nsdef"
def "nsdef:numis" "numis — the numis namespace."
set.add name="namespaces" id="nsdef:numis"
def "nsdef:nursing" "nursing — the nursing namespace."
set.add name="namespaces" id="nsdef:nursing"
def "nsdef:nut_type" "nut_type — the nut type namespace."
set.add name="namespaces" id="nsdef:nut_type"
def "nsdef:nutri" "nutri — the nutri namespace."
set.add name="namespaces" id="nsdef:nutri"
def "nsdef:nutrient" "nutrient — nutrients and micronutrients."
set.add name="namespaces" id="nsdef:nutrient"
def "nsdef:nutrisci" "nutrisci — the nutrisci namespace."
set.add name="namespaces" id="nsdef:nutrisci"
def "nsdef:nutrition" "nutrition — nutritional properties and bands."
set.add name="namespaces" id="nsdef:nutrition"
def "nsdef:obj" "obj — everyday objects."
set.add name="namespaces" id="nsdef:obj"
def "nsdef:occasion" "occasion — occasions and events food is served at."
set.add name="namespaces" id="nsdef:occasion"
def "nsdef:occupation" "occupation — occupations and sectors."
set.add name="namespaces" id="nsdef:occupation"
def "nsdef:occupation_sector" "occupation_sector — the occupation sector namespace."
set.add name="namespaces" id="nsdef:occupation_sector"
def "nsdef:oral" "oral — the oral namespace."
set.add name="namespaces" id="nsdef:oral"
def "nsdef:org" "org — the org namespace."
set.add name="namespaces" id="nsdef:org"
def "nsdef:os" "os — the os namespace."
set.add name="namespaces" id="nsdef:os"
def "nsdef:pairing" "pairing — food and drink pairings."
set.add name="namespaces" id="nsdef:pairing"
def "nsdef:paleo" "paleo — palaeography."
set.add name="namespaces" id="nsdef:paleo"
def "nsdef:papyr" "papyr — the papyr namespace."
set.add name="namespaces" id="nsdef:papyr"
def "nsdef:pedagogy" "pedagogy — the pedagogy namespace."
set.add name="namespaces" id="nsdef:pedagogy"
def "nsdef:people" "people — people and their flourishing eras."
set.add name="namespaces" id="nsdef:people"
def "nsdef:perf" "perf — the perf namespace."
set.add name="namespaces" id="nsdef:perf"
def "nsdef:person" "person — individual people."
set.add name="namespaces" id="nsdef:person"
def "nsdef:phil" "phil — philosophy."
set.add name="namespaces" id="nsdef:phil"
def "nsdef:phys" "phys — physics."
set.add name="namespaces" id="nsdef:phys"
def "nsdef:place" "place — places and place types."
set.add name="namespaces" id="nsdef:place"
def "nsdef:place_type" "place_type — the place type namespace."
set.add name="namespaces" id="nsdef:place_type"
def "nsdef:plantpart" "plantpart — the plantpart namespace."
set.add name="namespaces" id="nsdef:plantpart"
def "nsdef:plot" "plot — plot patterns and archetypes."
set.add name="namespaces" id="nsdef:plot"
def "nsdef:pm" "pm — the pm namespace."
set.add name="namespaces" id="nsdef:pm"
def "nsdef:polisci" "polisci — the polisci namespace."
set.add name="namespaces" id="nsdef:polisci"
def "nsdef:polthought" "polthought — the polthought namespace."
set.add name="namespaces" id="nsdef:polthought"
def "nsdef:polti" "polti — Polti's dramatic situations."
set.add name="namespaces" id="nsdef:polti"
def "nsdef:prog" "prog — the prog namespace."
set.add name="namespaces" id="nsdef:prog"
def "nsdef:psy" "psy — psychology."
set.add name="namespaces" id="nsdef:psy"
def "nsdef:psych" "psych — the psych namespace."
set.add name="namespaces" id="nsdef:psych"
def "nsdef:pub" "pub — the pub namespace."
set.add name="namespaces" id="nsdef:pub"
def "nsdef:quote" "quote — quotations."
set.add name="namespaces" id="nsdef:quote"
def "nsdef:rdm" "rdm — the rdm namespace."
set.add name="namespaces" id="nsdef:rdm"
def "nsdef:rec" "rec — recipe / tasting attributes (sweetness, acidity, heat)."
set.add name="namespaces" id="nsdef:rec"
def "nsdef:rec:color" "dna:color — innate / characteristic colour."
set.add name="namespaces" id="nsdef:rec:color"
def "nsdef:recipe" "recipe — recipes and their steps."
set.add name="namespaces" id="nsdef:recipe"
def "nsdef:ref" "ref — external references and citations."
set.add name="namespaces" id="nsdef:ref"
def "nsdef:region" "region — geographic and culinary regions."
set.add name="namespaces" id="nsdef:region"
def "nsdef:regulation" "regulation — the regulation namespace."
set.add name="namespaces" id="nsdef:regulation"
def "nsdef:rehab" "rehab — the rehab namespace."
set.add name="namespaces" id="nsdef:rehab"
def "nsdef:rel" "rel — relation-declaration namespace (rel:salient marks a relation as salient for a category)."
set.add name="namespaces" id="nsdef:rel"
def "nsdef:rel_kind" "rel_kind — the rel kind namespace."
set.add name="namespaces" id="nsdef:rel_kind"
def "nsdef:rel_polarity" "rel_polarity — the rel polarity namespace."
set.add name="namespaces" id="nsdef:rel_polarity"
def "nsdef:reldef" "reldef — relation-definition atoms; reldef:<rel> holds a relation's full-spelling description."
set.add name="namespaces" id="nsdef:reldef"
def "nsdef:religion" "religion — religions and traditions."
set.add name="namespaces" id="nsdef:religion"
def "nsdef:research" "research — the research namespace."
set.add name="namespaces" id="nsdef:research"
def "nsdef:resource" "resource — the resource namespace."
set.add name="namespaces" id="nsdef:resource"
def "nsdef:rhet" "rhet — the rhet namespace."
set.add name="namespaces" id="nsdef:rhet"
def "nsdef:risk" "risk — the risk namespace."
set.add name="namespaces" id="nsdef:risk"
def "nsdef:robotics" "robotics — the robotics namespace."
set.add name="namespaces" id="nsdef:robotics"
def "nsdef:sake" "sake — sake and grades."
set.add name="namespaces" id="nsdef:sake"
def "nsdef:school" "school — the school namespace."
set.add name="namespaces" id="nsdef:school"
def "nsdef:school_stage" "school_stage — the school stage namespace."
set.add name="namespaces" id="nsdef:school_stage"
def "nsdef:sci" "sci — science, general."
set.add name="namespaces" id="nsdef:sci"
def "nsdef:sci_hist" "sci_hist — history of science."
set.add name="namespaces" id="nsdef:sci_hist"
def "nsdef:score" "score — numeric scores attached via typed link (0-1 or scaled)."
set.add name="namespaces" id="nsdef:score"
def "nsdef:season" "season — seasons."
set.add name="namespaces" id="nsdef:season"
def "nsdef:seasonmap" "seasonmap — the seasonmap namespace."
set.add name="namespaces" id="nsdef:seasonmap"
def "nsdef:sem" "sem — the sem namespace."
set.add name="namespaces" id="nsdef:sem"
def "nsdef:semio" "semio — semiotics."
set.add name="namespaces" id="nsdef:semio"
def "nsdef:sense" "sense — sensory descriptors."
set.add name="namespaces" id="nsdef:sense"
def "nsdef:sensory" "sensory — the sensory namespace."
set.add name="namespaces" id="nsdef:sensory"
def "nsdef:service" "service — the service namespace."
set.add name="namespaces" id="nsdef:service"
def "nsdef:shape" "shape — the shape namespace."
set.add name="namespaces" id="nsdef:shape"
def "nsdef:si" "si — the si namespace."
set.add name="namespaces" id="nsdef:si"
def "nsdef:sigil" "sigil — the sigil namespace."
set.add name="namespaces" id="nsdef:sigil"
def "nsdef:sky" "sky — the sky namespace."
set.add name="namespaces" id="nsdef:sky"
def "nsdef:social" "social — the social namespace."
set.add name="namespaces" id="nsdef:social"
def "nsdef:society" "society — the society namespace."
set.add name="namespaces" id="nsdef:society"
def "nsdef:socio" "socio — sociology."
set.add name="namespaces" id="nsdef:socio"
def "nsdef:socwork" "socwork — the socwork namespace."
set.add name="namespaces" id="nsdef:socwork"
def "nsdef:source" "source — the source namespace."
set.add name="namespaces" id="nsdef:source"
def "nsdef:spacefood" "spacefood — the spacefood namespace."
set.add name="namespaces" id="nsdef:spacefood"
def "nsdef:special" "special — the special namespace."
set.add name="namespaces" id="nsdef:special"
def "nsdef:spice_part" "spice_part — the spice part namespace."
set.add name="namespaces" id="nsdef:spice_part"
def "nsdef:spirit" "spirit — spirits and distilled drinks."
set.add name="namespaces" id="nsdef:spirit"
def "nsdef:sportsci" "sportsci — the sportsci namespace."
set.add name="namespaces" id="nsdef:sportsci"
def "nsdef:stationery" "stationery — the stationery namespace."
set.add name="namespaces" id="nsdef:stationery"
def "nsdef:status" "status — the status namespace."
set.add name="namespaces" id="nsdef:status"
def "nsdef:supply" "supply — the supply namespace."
set.add name="namespaces" id="nsdef:supply"
def "nsdef:sus" "sus — the sus namespace."
set.add name="namespaces" id="nsdef:sus"
def "nsdef:sys" "sys — built-in structural relations and system atoms."
set.add name="namespaces" id="nsdef:sys"
def "nsdef:tale" "tale — folk tales and fables."
set.add name="namespaces" id="nsdef:tale"
def "nsdef:tasting" "tasting — the tasting namespace."
set.add name="namespaces" id="nsdef:tasting"
def "nsdef:tcm" "tcm — Traditional Chinese Medicine properties."
set.add name="namespaces" id="nsdef:tcm"
def "nsdef:tea" "tea — teas."
set.add name="namespaces" id="nsdef:tea"
def "nsdef:teaching" "teaching — the teaching namespace."
set.add name="namespaces" id="nsdef:teaching"
def "nsdef:teb" "teb — the teb namespace."
set.add name="namespaces" id="nsdef:teb"
def "nsdef:tech" "tech — technology."
set.add name="namespaces" id="nsdef:tech"
def "nsdef:technique" "technique — the technique namespace."
set.add name="namespaces" id="nsdef:technique"
def "nsdef:technique_param" "technique_param — the technique param namespace."
set.add name="namespaces" id="nsdef:technique_param"
def "nsdef:technique_type" "technique_type — the technique type namespace."
set.add name="namespaces" id="nsdef:technique_type"
def "nsdef:textanal" "textanal — the textanal namespace."
set.add name="namespaces" id="nsdef:textanal"
def "nsdef:textcrit" "textcrit — the textcrit namespace."
set.add name="namespaces" id="nsdef:textcrit"
def "nsdef:theme" "theme — the theme namespace."
set.add name="namespaces" id="nsdef:theme"
def "nsdef:thesaurus" "thesaurus — thesaurus relations and cross-references."
set.add name="namespaces" id="nsdef:thesaurus"
def "nsdef:toiletry" "toiletry — the toiletry namespace."
set.add name="namespaces" id="nsdef:toiletry"
def "nsdef:tool" "tool — tools and implements."
set.add name="namespaces" id="nsdef:tool"
def "nsdef:toy" "toy — the toy namespace."
set.add name="namespaces" id="nsdef:toy"
def "nsdef:toy_type" "toy_type — the toy type namespace."
set.add name="namespaces" id="nsdef:toy_type"
def "nsdef:tradition" "tradition — the tradition namespace."
set.add name="namespaces" id="nsdef:tradition"
def "nsdef:transport" "transport — the transport namespace."
set.add name="namespaces" id="nsdef:transport"
def "nsdef:unit" "unit — the unit namespace."
set.add name="namespaces" id="nsdef:unit"
def "nsdef:verb" "verb — verbs and the objects they act on."
set.add name="namespaces" id="nsdef:verb"
def "nsdef:verb_class" "verb_class — the verb class namespace."
set.add name="namespaces" id="nsdef:verb_class"
def "nsdef:war" "war — warfare and conflicts."
set.add name="namespaces" id="nsdef:war"
def "nsdef:weather" "weather — weather and climate phenomena."
set.add name="namespaces" id="nsdef:weather"
def "nsdef:whiskey" "whiskey — whiskies and types."
set.add name="namespaces" id="nsdef:whiskey"
def "nsdef:wine" "wine — wines, regions and grapes."
set.add name="namespaces" id="nsdef:wine"
def "nsdef:word" "word — WordNet-derived vocabulary and proto-words."
set.add name="namespaces" id="nsdef:word"
def "nsdef:word:color" "word:color — colour senses of a word."
set.add name="namespaces" id="nsdef:word:color"
def "nsdef:word:sense" "word:sense — sense descriptors of a word."
set.add name="namespaces" id="nsdef:word:sense"
def "nsdef:work" "work — the work namespace."
set.add name="namespaces" id="nsdef:work"
