# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.

def "reldef:@abundant_in" "abundant in — 'abundant in' relation asserted between two atoms."
set.add name="rels" id="reldef:@abundant_in"
def "reldef:@accelerates" "accelerates — 'accelerates' relation asserted between two atoms."
set.add name="rels" id="reldef:@accelerates"
def "reldef:@accelerates_legacy_of" "accelerates legacy of — 'accelerates legacy of' relation asserted between two atoms."
set.add name="rels" id="reldef:@accelerates_legacy_of"
def "reldef:@adopted_by" "adopted by — 'adopted by' relation asserted between two atoms."
set.add name="rels" id="reldef:@adopted_by"
def "reldef:@analogous_to" "analogous to — 'analogous to' relation asserted between two atoms."
set.add name="rels" id="reldef:@analogous_to"
def "reldef:@associated_with" "associated with — 'associated with' relation asserted between two atoms."
set.add name="rels" id="reldef:@associated_with"
def "reldef:@can_contain" "can contain — 'can contain' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_contain"
def "reldef:@can_function_as" "can function as — 'can function as' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_function_as"
def "reldef:@can_take_form_as" "can take form as — 'can take form as' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_take_form_as"
def "reldef:@challenges" "challenges — 'challenges' relation asserted between two atoms."
set.add name="rels" id="reldef:@challenges"
def "reldef:@complicates" "complicates — 'complicates' relation asserted between two atoms."
set.add name="rels" id="reldef:@complicates"
def "reldef:@composed_of" "composed of — 'composed of' relation asserted between two atoms."
set.add name="rels" id="reldef:@composed_of"
def "reldef:@constructs" "constructs — 'constructs' relation asserted between two atoms."
set.add name="rels" id="reldef:@constructs"
def "reldef:@consumes" "consumes — 'consumes' relation asserted between two atoms."
set.add name="rels" id="reldef:@consumes"
def "reldef:@contains" "contains — 'contains' relation asserted between two atoms."
set.add name="rels" id="reldef:@contains"
def "reldef:@contradicts" "contradicts — 'contradicts' relation asserted between two atoms."
set.add name="rels" id="reldef:@contradicts"
def "reldef:@contrasts_with" "contrasts with — 'contrasts with' relation asserted between two atoms."
set.add name="rels" id="reldef:@contrasts_with"
def "reldef:@creates" "creates — 'creates' relation asserted between two atoms."
set.add name="rels" id="reldef:@creates"
def "reldef:@decodes" "decodes — 'decodes' relation asserted between two atoms."
set.add name="rels" id="reldef:@decodes"
def "reldef:@demonstrates" "demonstrates — 'demonstrates' relation asserted between two atoms."
set.add name="rels" id="reldef:@demonstrates"
def "reldef:@depends_on" "depends on — 'depends on' relation asserted between two atoms."
set.add name="rels" id="reldef:@depends_on"
def "reldef:@derived_from" "derived from — 'derived from' relation asserted between two atoms."
set.add name="rels" id="reldef:@derived_from"
def "reldef:@destroys" "destroys — 'destroys' relation asserted between two atoms."
set.add name="rels" id="reldef:@destroys"
def "reldef:@develops" "develops — 'develops' relation asserted between two atoms."
set.add name="rels" id="reldef:@develops"
def "reldef:@directs_toward" "directs toward — 'directs toward' relation asserted between two atoms."
set.add name="rels" id="reldef:@directs_toward"
def "reldef:@discovers_accidentally" "discovers accidentally — 'discovers accidentally' relation asserted between two atoms."
set.add name="rels" id="reldef:@discovers_accidentally"
def "reldef:@doped_with" "doped with — 'doped with' relation asserted between two atoms."
set.add name="rels" id="reldef:@doped_with"
def "reldef:@elevates" "elevates — 'elevates' relation asserted between two atoms."
set.add name="rels" id="reldef:@elevates"
def "reldef:@embodied_by" "embodied by — 'embodied by' relation asserted between two atoms."
set.add name="rels" id="reldef:@embodied_by"
def "reldef:@enables" "enables — 'enables' relation asserted between two atoms."
set.add name="rels" id="reldef:@enables"
def "reldef:@encounters" "encounters — 'encounters' relation asserted between two atoms."
set.add name="rels" id="reldef:@encounters"
def "reldef:@enhances" "enhances — 'enhances' relation asserted between two atoms."
set.add name="rels" id="reldef:@enhances"
def "reldef:@essential_for" "essential for — 'essential for' relation asserted between two atoms."
set.add name="rels" id="reldef:@essential_for"
def "reldef:@establishes" "establishes — 'establishes' relation asserted between two atoms."
set.add name="rels" id="reldef:@establishes"
def "reldef:@evaluates" "evaluates — 'evaluates' relation asserted between two atoms."
set.add name="rels" id="reldef:@evaluates"
def "reldef:@examines" "examines — 'examines' relation asserted between two atoms."
set.add name="rels" id="reldef:@examines"
def "reldef:@exhibits" "exhibits — 'exhibits' relation asserted between two atoms."
set.add name="rels" id="reldef:@exhibits"
def "reldef:@exhibits_maximum" "exhibits maximum — 'exhibits maximum' relation asserted between two atoms."
set.add name="rels" id="reldef:@exhibits_maximum"
def "reldef:@exhibits_minimum" "exhibits minimum — 'exhibits minimum' relation asserted between two atoms."
set.add name="rels" id="reldef:@exhibits_minimum"
def "reldef:@explains" "explains — 'explains' relation asserted between two atoms."
set.add name="rels" id="reldef:@explains"
def "reldef:@extends" "extends — 'extends' relation asserted between two atoms."
set.add name="rels" id="reldef:@extends"
def "reldef:@follows" "follows — 'follows' relation asserted between two atoms."
set.add name="rels" id="reldef:@follows"
def "reldef:@foundational_for" "foundational for — 'foundational for' relation asserted between two atoms."
set.add name="rels" id="reldef:@foundational_for"
def "reldef:@fulfills" "fulfills — 'fulfills' relation asserted between two atoms."
set.add name="rels" id="reldef:@fulfills"
def "reldef:@functions_as" "functions as — 'functions as' relation asserted between two atoms."
set.add name="rels" id="reldef:@functions_as"
def "reldef:@fuses_with" "fuses with — 'fuses with' relation asserted between two atoms."
set.add name="rels" id="reldef:@fuses_with"
def "reldef:@grounds" "grounds — 'grounds' relation asserted between two atoms."
set.add name="rels" id="reldef:@grounds"
def "reldef:@has_emotion" "has emotion — 'has emotion' relation asserted between two atoms."
set.add name="rels" id="reldef:@has_emotion"
def "reldef:@historical_basis_of" "historical basis of — 'historical basis of' relation asserted between two atoms."
set.add name="rels" id="reldef:@historical_basis_of"
def "reldef:@illustrates" "illustrates — 'illustrates' relation asserted between two atoms."
set.add name="rels" id="reldef:@illustrates"
def "reldef:@increases" "increases — 'increases' relation asserted between two atoms."
set.add name="rels" id="reldef:@increases"
def "reldef:@influences" "influences — 'influences' relation asserted between two atoms."
set.add name="rels" id="reldef:@influences"
def "reldef:@initiates" "initiates — 'initiates' relation asserted between two atoms."
set.add name="rels" id="reldef:@initiates"
def "reldef:@integrates" "integrates — 'integrates' relation asserted between two atoms."
set.add name="rels" id="reldef:@integrates"
def "reldef:@interprets" "interprets — 'interprets' relation asserted between two atoms."
set.add name="rels" id="reldef:@interprets"
def "reldef:@invents" "invents — 'invents' relation asserted between two atoms."
set.add name="rels" id="reldef:@invents"
def "reldef:@lowers" "lowers — 'lowers' relation asserted between two atoms."
set.add name="rels" id="reldef:@lowers"
def "reldef:@maintained_by" "maintained by — 'maintained by' relation asserted between two atoms."
set.add name="rels" id="reldef:@maintained_by"
def "reldef:@may_be" "may be — 'may be' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_be"
def "reldef:@may_legitimize" "may legitimize — 'may legitimize' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_legitimize"
def "reldef:@may_produce" "may produce — 'may produce' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_produce"
def "reldef:@may_use" "may use — 'may use' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_use"
def "reldef:@measured_by" "measured by — 'measured by' relation asserted between two atoms."
set.add name="rels" id="reldef:@measured_by"
def "reldef:@measures_impact_of" "measures impact of — 'measures impact of' relation asserted between two atoms."
set.add name="rels" id="reldef:@measures_impact_of"
def "reldef:@moderates" "moderates — 'moderates' relation asserted between two atoms."
set.add name="rels" id="reldef:@moderates"
def "reldef:@motivates" "motivates — 'motivates' relation asserted between two atoms."
set.add name="rels" id="reldef:@motivates"
def "reldef:@optimizes" "optimizes — 'optimizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@optimizes"
def "reldef:@polymorph_of" "polymorph of — 'polymorph of' relation asserted between two atoms."
set.add name="rels" id="reldef:@polymorph_of"
def "reldef:@popularized_by" "popularized by — 'popularized by' relation asserted between two atoms."
set.add name="rels" id="reldef:@popularized_by"
def "reldef:@precedes" "precedes — 'precedes' relation asserted between two atoms."
set.add name="rels" id="reldef:@precedes"
def "reldef:@precedes_conceptually" "precedes conceptually — 'precedes conceptually' relation asserted between two atoms."
set.add name="rels" id="reldef:@precedes_conceptually"
def "reldef:@preserved_by" "preserved by — 'preserved by' relation asserted between two atoms."
set.add name="rels" id="reldef:@preserved_by"
def "reldef:@preserves" "preserves — 'preserves' relation asserted between two atoms."
set.add name="rels" id="reldef:@preserves"
def "reldef:@prevents" "prevents — 'prevents' relation asserted between two atoms."
set.add name="rels" id="reldef:@prevents"
def "reldef:@produces" "produces — 'produces' relation asserted between two atoms."
set.add name="rels" id="reldef:@produces"
def "reldef:@promotes" "promotes — 'promotes' relation asserted between two atoms."
set.add name="rels" id="reldef:@promotes"
def "reldef:@provides" "provides — 'provides' relation asserted between two atoms."
set.add name="rels" id="reldef:@provides"
def "reldef:@qualifies" "qualifies — 'qualifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@qualifies"
def "reldef:@questions" "questions — 'questions' relation asserted between two atoms."
set.add name="rels" id="reldef:@questions"
def "reldef:@realizes" "realizes — 'realizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@realizes"
def "reldef:@reduces" "reduces — 'reduces' relation asserted between two atoms."
set.add name="rels" id="reldef:@reduces"
def "reldef:@refines" "refines — 'refines' relation asserted between two atoms."
set.add name="rels" id="reldef:@refines"
def "reldef:@regulates_absorption" "regulates absorption — 'regulates absorption' relation asserted between two atoms."
set.add name="rels" id="reldef:@regulates_absorption"
def "reldef:@reinforces" "reinforces — 'reinforces' relation asserted between two atoms."
set.add name="rels" id="reldef:@reinforces"
def "reldef:@related_to" "related to — 'related to' relation asserted between two atoms."
set.add name="rels" id="reldef:@related_to"
def "reldef:@requires" "requires — 'requires' relation asserted between two atoms."
set.add name="rels" id="reldef:@requires"
def "reldef:@resembles" "resembles — 'resembles' relation asserted between two atoms."
set.add name="rels" id="reldef:@resembles"
def "reldef:@resists" "resists — 'resists' relation asserted between two atoms."
set.add name="rels" id="reldef:@resists"
def "reldef:@resolves" "resolves — 'resolves' relation asserted between two atoms."
set.add name="rels" id="reldef:@resolves"
def "reldef:@rich_in" "rich in — 'rich in' relation asserted between two atoms."
set.add name="rels" id="reldef:@rich_in"
def "reldef:@seeks" "seeks — 'seeks' relation asserted between two atoms."
set.add name="rels" id="reldef:@seeks"
def "reldef:@shapes" "shapes — 'shapes' relation asserted between two atoms."
set.add name="rels" id="reldef:@shapes"
def "reldef:@structures" "structures — 'structures' relation asserted between two atoms."
set.add name="rels" id="reldef:@structures"
def "reldef:@supports" "supports — 'supports' relation asserted between two atoms."
set.add name="rels" id="reldef:@supports"
def "reldef:@threatens" "threatens — 'threatens' relation asserted between two atoms."
set.add name="rels" id="reldef:@threatens"
def "reldef:@transmits_globally" "transmits globally — 'transmits globally' relation asserted between two atoms."
set.add name="rels" id="reldef:@transmits_globally"
def "reldef:@transmitted_via" "transmitted via — 'transmitted via' relation asserted between two atoms."
set.add name="rels" id="reldef:@transmitted_via"
def "reldef:@triggers" "triggers — 'triggers' relation asserted between two atoms."
set.add name="rels" id="reldef:@triggers"
def "reldef:@undergoes" "undergoes — 'undergoes' relation asserted between two atoms."
set.add name="rels" id="reldef:@undergoes"
def "reldef:@uses" "uses — 'uses' relation asserted between two atoms."
set.add name="rels" id="reldef:@uses"
def "reldef:@utilizes" "utilizes — 'utilizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@utilizes"
def "reldef:@violates" "violates — 'violates' relation asserted between two atoms."
set.add name="rels" id="reldef:@violates"
def "reldef:acad:outranks" "acad outranks — acad relation (acad outranks)."
set.add name="rels" id="reldef:acad:outranks"
def "reldef:admin:contains" "admin contains — admin relation (admin contains)."
set.add name="rels" id="reldef:admin:contains"
def "reldef:admin:governs" "admin governs — admin relation (admin governs)."
set.add name="rels" id="reldef:admin:governs"
def "reldef:admin:kind_of" "admin kind of — admin relation (admin kind of)."
set.add name="rels" id="reldef:admin:kind_of"
def "reldef:admin:resident_of" "admin resident of — admin relation (admin resident of)."
set.add name="rels" id="reldef:admin:resident_of"
def "reldef:admin:scale" "admin scale — admin relation (admin scale)."
set.add name="rels" id="reldef:admin:scale"
def "reldef:admin:seat_of" "admin seat of — admin relation (admin seat of)."
set.add name="rels" id="reldef:admin:seat_of"
def "reldef:admin:within" "admin within — admin relation (admin within)."
set.add name="rels" id="reldef:admin:within"
def "reldef:allergen:excludes" "excludes allergen — the item excludes the target allergen."
set.add name="rels" id="reldef:allergen:excludes"
def "reldef:arch:derived_from" "derived from — the architectural style derives from the target."
set.add name="rels" id="reldef:arch:derived_from"
def "reldef:arch:influenced_by" "influenced by — the architectural style was influenced by the target."
set.add name="rels" id="reldef:arch:influenced_by"
def "reldef:arch:reacts_against" "reacts against — the architectural style reacts against the target."
set.add name="rels" id="reldef:arch:reacts_against"
def "reldef:art:derived_from" "derived from — the style derives from the target."
set.add name="rels" id="reldef:art:derived_from"
def "reldef:art:influenced_by" "influenced by — the artist or movement was influenced by the target."
set.add name="rels" id="reldef:art:influenced_by"
def "reldef:art:of_period" "of period — the work belongs to the target art period."
set.add name="rels" id="reldef:art:of_period"
def "reldef:art:preceded_by" "preceded by — the movement was preceded by the target."
set.add name="rels" id="reldef:art:preceded_by"
def "reldef:art:reacts_against" "reacts against — the movement reacts against the target."
set.add name="rels" id="reldef:art:reacts_against"
def "reldef:ayurveda:aggravates" "ayurveda aggravates — ayurveda relation (ayurveda aggravates)."
set.add name="rels" id="reldef:ayurveda:aggravates"
def "reldef:ayurveda:pacifies" "ayurveda pacifies — ayurveda relation (ayurveda pacifies)."
set.add name="rels" id="reldef:ayurveda:pacifies"
def "reldef:ayurveda:virya" "ayurveda virya — ayurveda relation (ayurveda virya)."
set.add name="rels" id="reldef:ayurveda:virya"
def "reldef:bio:allergen" "allergen — marks a substance as a biological allergy trigger."
set.add name="rels" id="reldef:bio:allergen"
def "reldef:bio:climate" "climate — the climate in which the organism grows."
set.add name="rels" id="reldef:bio:climate"
def "reldef:bio:dietflag" "diet flag — a dietary classification flag for the food."
set.add name="rels" id="reldef:bio:dietflag"
def "reldef:bio:fat_source" "fat source — the source of dietary fat."
set.add name="rels" id="reldef:bio:fat_source"
def "reldef:bio:flesh_type" "flesh type — the type of flesh (e.g. white / red / oily)."
set.add name="rels" id="reldef:bio:flesh_type"
def "reldef:bio:fruit_type" "fruit type — the botanical fruit type (e.g. pome, drupe, berry)."
set.add name="rels" id="reldef:bio:fruit_type"
def "reldef:bio:grain_type" "grain type — the cereal / grain type."
set.add name="rels" id="reldef:bio:grain_type"
def "reldef:bio:habitat" "habitat — the natural habitat of the organism."
set.add name="rels" id="reldef:bio:habitat"
def "reldef:bio:herb_type" "herb type — the culinary / botanical herb type."
set.add name="rels" id="reldef:bio:herb_type"
def "reldef:bio:husbandry" "husbandry — the animal husbandry or rearing method."
set.add name="rels" id="reldef:bio:husbandry"
def "reldef:bio:legume_type" "legume type — the botanical legume type."
set.add name="rels" id="reldef:bio:legume_type"
def "reldef:bio:nut_type" "nut type — the botanical nut type."
set.add name="rels" id="reldef:bio:nut_type"
def "reldef:bio:plant_part" "plant part — the edible or used part of the plant."
set.add name="rels" id="reldef:bio:plant_part"
def "reldef:bio:source" "source — the biological source the food is derived from."
set.add name="rels" id="reldef:bio:source"
def "reldef:bio:source_primary" "primary source — the main biological source of the food."
set.add name="rels" id="reldef:bio:source_primary"
def "reldef:broadcast:mode" "broadcast mode — broadcast relation (broadcast mode)."
set.add name="rels" id="reldef:broadcast:mode"
def "reldef:broadcast:received_by" "broadcast received by — broadcast relation (broadcast received by)."
set.add name="rels" id="reldef:broadcast:received_by"
def "reldef:c:deepens" "c deepens — c relation (c deepens)."
set.add name="rels" id="reldef:c:deepens"
def "reldef:calc:associated_with" "associated with — a computed or curated association between two atoms."
set.add name="rels" id="reldef:calc:associated_with"
def "reldef:calc:has_emotion" "has emotion — the source evokes or carries the target emotion."
set.add name="rels" id="reldef:calc:has_emotion"
def "reldef:chrono:before" "before — the source precedes the target in time."
set.add name="rels" id="reldef:chrono:before"
def "reldef:civic:function" "civic function — civic relation (civic function)."
set.add name="rels" id="reldef:civic:function"
def "reldef:civic:held_at" "civic held at — civic relation (civic held at)."
set.add name="rels" id="reldef:civic:held_at"
def "reldef:civic:operated_by" "civic operated by — civic relation (civic operated by)."
set.add name="rels" id="reldef:civic:operated_by"
def "reldef:civic:role_at" "civic role at — civic relation (civic role at)."
set.add name="rels" id="reldef:civic:role_at"
def "reldef:civic:serves" "civic serves — civic relation (civic serves)."
set.add name="rels" id="reldef:civic:serves"
def "reldef:civic:supplies" "civic supplies — civic relation (civic supplies)."
set.add name="rels" id="reldef:civic:supplies"
def "reldef:clothing:formality" "clothing formality — clothing relation (clothing formality)."
set.add name="rels" id="reldef:clothing:formality"
def "reldef:clothing:zone" "clothing zone — clothing relation (clothing zone)."
set.add name="rels" id="reldef:clothing:zone"
def "reldef:concept:includes" "concept includes — concept relation (concept includes)."
set.add name="rels" id="reldef:concept:includes"
def "reldef:concept:related" "concept related — concept relation (concept related)."
set.add name="rels" id="reldef:concept:related"
def "reldef:concept:requires" "concept requires — concept relation (concept requires)."
set.add name="rels" id="reldef:concept:requires"
def "reldef:crosscut:manifests_in" "manifests in — the cross-cutting theme manifests in the target field."
set.add name="rels" id="reldef:crosscut:manifests_in"
def "reldef:cuisine:signature" "signature dish — a signature dish of the cuisine."
set.add name="rels" id="reldef:cuisine:signature"
def "reldef:cuisine:staple" "staple — a staple food of the cuisine."
set.add name="rels" id="reldef:cuisine:staple"
def "reldef:culinary:aging" "aging — the aging / maturation treatment."
set.add name="rels" id="reldef:culinary:aging"
def "reldef:culinary:common" "commonly used with — a common culinary pairing."
set.add name="rels" id="reldef:culinary:common"
def "reldef:culinary:component" "component — a culinary component of the dish."
set.add name="rels" id="reldef:culinary:component"
def "reldef:culinary:condiment_type" "condiment type — the class of condiment."
set.add name="rels" id="reldef:culinary:condiment_type"
def "reldef:culinary:cut_type" "cut type — the butchery / knife cut type."
set.add name="rels" id="reldef:culinary:cut_type"
def "reldef:culinary:ferment_type" "ferment type — the fermentation type."
set.add name="rels" id="reldef:culinary:ferment_type"
def "reldef:culinary:heat_level" "heat level — the cooking heat level."
set.add name="rels" id="reldef:culinary:heat_level"
def "reldef:culinary:region" "region — the culinary region of origin."
set.add name="rels" id="reldef:culinary:region"
def "reldef:culinary:technique_type" "technique type — the cooking technique class used."
set.add name="rels" id="reldef:culinary:technique_type"
def "reldef:culinary:use" "culinary use — the typical culinary use of the ingredient."
set.add name="rels" id="reldef:culinary:use"
def "reldef:deity:embodies" "embodies — the deity embodies the target domain or quality."
set.add name="rels" id="reldef:deity:embodies"
def "reldef:deity:worshipped_in" "worshipped in — the deity was worshipped in the target culture or place."
set.add name="rels" id="reldef:deity:worshipped_in"
def "reldef:dh:area" "area — the digital-humanities area of the work."
set.add name="rels" id="reldef:dh:area"
def "reldef:dh:bridges" "bridges — the digital-humanities method bridges the two fields."
set.add name="rels" id="reldef:dh:bridges"
def "reldef:dh:can_analyse" "can analyse — the method can analyse the target material."
set.add name="rels" id="reldef:dh:can_analyse"
def "reldef:dh:grounded_in" "grounded in — the method is grounded in the target discipline."
set.add name="rels" id="reldef:dh:grounded_in"
def "reldef:dh:serves" "serves — the method serves the target scholarly purpose."
set.add name="rels" id="reldef:dh:serves"
def "reldef:diet:emphasized_in" "emphasised in — the food is emphasised in the target diet."
set.add name="rels" id="reldef:diet:emphasized_in"
def "reldef:diet:excludes" "excludes — the diet excludes the target food."
set.add name="rels" id="reldef:diet:excludes"
def "reldef:diet:limited_in" "limited in — the food is limited in the target diet."
set.add name="rels" id="reldef:diet:limited_in"
def "reldef:dietlaw:forbids" "forbids — the dietary law forbids the target food."
set.add name="rels" id="reldef:dietlaw:forbids"
def "reldef:econ:field" "field — the economics sub-field of the concept."
set.add name="rels" id="reldef:econ:field"
def "reldef:eng:applies" "eng applies — eng relation (eng applies)."
set.add name="rels" id="reldef:eng:applies"
def "reldef:geo:borders" "borders — the place shares a border with the target place."
set.add name="rels" id="reldef:geo:borders"
def "reldef:geo:in_country" "in country — the place lies within the target country."
set.add name="rels" id="reldef:geo:in_country"
def "reldef:geo:instance_of" "instance of — the place is an instance of the target place type."
set.add name="rels" id="reldef:geo:instance_of"
def "reldef:geo:member_of" "member of — the place is a member of the target grouping."
set.add name="rels" id="reldef:geo:member_of"
def "reldef:geo:official_language" "official language — the target language is official in the place."
set.add name="rels" id="reldef:geo:official_language"
def "reldef:gov:based_at" "gov based at — gov relation (gov based at)."
set.add name="rels" id="reldef:gov:based_at"
def "reldef:gov:delivers" "gov delivers — gov relation (gov delivers)."
set.add name="rels" id="reldef:gov:delivers"
def "reldef:gov:pays" "gov pays — gov relation (gov pays)."
set.add name="rels" id="reldef:gov:pays"
def "reldef:gov:provided_by" "gov provided by — gov relation (gov provided by)."
set.add name="rels" id="reldef:gov:provided_by"
def "reldef:gov:reaches" "gov reaches — gov relation (gov reaches)."
set.add name="rels" id="reldef:gov:reaches"
def "reldef:gov:regulated_by" "gov regulated by — gov relation (gov regulated by)."
set.add name="rels" id="reldef:gov:regulated_by"
def "reldef:gov:used_by" "gov used by — gov relation (gov used by)."
set.add name="rels" id="reldef:gov:used_by"
def "reldef:gov:works_for" "gov works for — gov relation (gov works for)."
set.add name="rels" id="reldef:gov:works_for"
def "reldef:has_emotion" "has emotion — the source evokes or carries the target emotion."
set.add name="rels" id="reldef:has_emotion"
def "reldef:hist:concurrent_with" "concurrent with — the two entities were concurrent in time."
set.add name="rels" id="reldef:hist:concurrent_with"
def "reldef:hist:era_of" "era of — the era of the target polity or culture."
set.add name="rels" id="reldef:hist:era_of"
def "reldef:hist:flourished_in" "flourished in — the polity or culture flourished in the target era."
set.add name="rels" id="reldef:hist:flourished_in"
def "reldef:hist:occurred_in" "occurred in — the event occurred in the target place or time."
set.add name="rels" id="reldef:hist:occurred_in"
def "reldef:hist:period_of" "period of — the event belongs to the target historical period."
set.add name="rels" id="reldef:hist:period_of"
def "reldef:hist:territory_now" "territory now — the historical territory now corresponds to the target place."
set.add name="rels" id="reldef:hist:territory_now"
def "reldef:home:adjacent_to" "adjacent to — the room adjoins the target room."
set.add name="rels" id="reldef:home:adjacent_to"
def "reldef:home:has_room" "has room — the dwelling contains the target room."
set.add name="rels" id="reldef:home:has_room"
def "reldef:home:located_in" "located in — the item is normally located in the target room or zone."
set.add name="rels" id="reldef:home:located_in"
def "reldef:home:used_for" "used for — the item is used for the target purpose."
set.add name="rels" id="reldef:home:used_for"
def "reldef:home:zone" "zone — the item belongs to the target home zone."
set.add name="rels" id="reldef:home:zone"
def "reldef:in_season" "in season — the ingredient is in season during the target period."
set.add name="rels" id="reldef:in_season"
def "reldef:infra:backed_by" "infra backed by — infra relation (infra backed by)."
set.add name="rels" id="reldef:infra:backed_by"
def "reldef:infra:delivered_via" "infra delivered via — infra relation (infra delivered via)."
set.add name="rels" id="reldef:infra:delivered_via"
def "reldef:infra:metered_by" "infra metered by — infra relation (infra metered by)."
set.add name="rels" id="reldef:infra:metered_by"
def "reldef:infra:powered_by" "infra powered by — infra relation (infra powered by)."
set.add name="rels" id="reldef:infra:powered_by"
def "reldef:kin:gender" "kin gender — kin relation (kin gender)."
set.add name="rels" id="reldef:kin:gender"
def "reldef:kin:generation" "kin generation — kin relation (kin generation)."
set.add name="rels" id="reldef:kin:generation"
def "reldef:kin:line" "kin line — kin relation (kin line)."
set.add name="rels" id="reldef:kin:line"
def "reldef:kin:role_of" "kin role of — kin relation (kin role of)."
set.add name="rels" id="reldef:kin:role_of"
def "reldef:knowledge:in_branch" "in branch — the concept sits in the target branch of knowledge."
set.add name="rels" id="reldef:knowledge:in_branch"
def "reldef:law:codified_in" "codified in — the law was codified in the target code or era."
set.add name="rels" id="reldef:law:codified_in"
def "reldef:law:contrasts_with" "contrasts with — the legal concept contrasts with the target."
set.add name="rels" id="reldef:law:contrasts_with"
def "reldef:law:derived_from" "derived from — the law derives from the target source."
set.add name="rels" id="reldef:law:derived_from"
def "reldef:law:influenced_by" "influenced by — the legal tradition was influenced by the target."
set.add name="rels" id="reldef:law:influenced_by"
def "reldef:law:source_of" "source of — the source is a source of the target law."
set.add name="rels" id="reldef:law:source_of"
def "reldef:lineage:begat" "lineage begat — lineage relation (lineage begat)."
set.add name="rels" id="reldef:lineage:begat"
def "reldef:ling:feeds_into" "ling feeds into — ling relation (ling feeds into)."
set.add name="rels" id="reldef:ling:feeds_into"
def "reldef:media:depicts" "depicts — the media atom depicts the target subject."
set.add name="rels" id="reldef:media:depicts"
def "reldef:media:video" "video — links the atom to a video resource."
set.add name="rels" id="reldef:media:video"
def "reldef:nutrition:fodmap" "FODMAP level — the food's FODMAP fermentability band."
set.add name="rels" id="reldef:nutrition:fodmap"
def "reldef:nutrition:glycemic" "glycaemic level — the food's glycaemic index band."
set.add name="rels" id="reldef:nutrition:glycemic"
def "reldef:obj_durability" "durability — the object's durability class."
set.add name="rels" id="reldef:obj_durability"
def "reldef:occasion:served_at" "served at — the dish is served at the target occasion."
set.add name="rels" id="reldef:occasion:served_at"
def "reldef:occupation:field" "occupation field — occupation relation (occupation field)."
set.add name="rels" id="reldef:occupation:field"
def "reldef:occupation:sector" "occupation sector — occupation relation (occupation sector)."
set.add name="rels" id="reldef:occupation:sector"
def "reldef:occupation:works_at" "occupation works at — occupation relation (occupation works at)."
set.add name="rels" id="reldef:occupation:works_at"
def "reldef:paleo:script_studied" "paleo script studied — paleo relation (paleo script studied)."
set.add name="rels" id="reldef:paleo:script_studied"
def "reldef:people:flourished_in" "flourished in — the person flourished in the target era or place."
set.add name="rels" id="reldef:people:flourished_in"
def "reldef:people:related_to" "related to — a relation between the two people."
set.add name="rels" id="reldef:people:related_to"
def "reldef:person:active_in" "active in — the person was active in the target field or era."
set.add name="rels" id="reldef:person:active_in"
def "reldef:person:belongs_to_school" "belongs to school — the person belongs to the target school of thought."
set.add name="rels" id="reldef:person:belongs_to_school"
def "reldef:person:from_country" "from country — the person's country of origin."
set.add name="rels" id="reldef:person:from_country"
def "reldef:person:lived_in" "lived in — the person lived in the target place."
set.add name="rels" id="reldef:person:lived_in"
def "reldef:person:related_to" "related to — a relation between the two people."
set.add name="rels" id="reldef:person:related_to"
def "reldef:person:worked_in" "worked in — the person worked in the target field or place."
set.add name="rels" id="reldef:person:worked_in"
def "reldef:rec:acidity" "acidity — perceived sourness / acid intensity on a 0-10 scale."
set.add name="rels" id="reldef:rec:acidity"
def "reldef:rec:color" "colour — the characteristic colour of the ingredient."
set.add name="rels" id="reldef:rec:color"
def "reldef:rec:heat" "heat — perceived pungency / chilli heat on a 0-10 scale."
set.add name="rels" id="reldef:rec:heat"
def "reldef:rec:sweetness" "sweetness — perceived sugar intensity on a 0-10 scale."
set.add name="rels" id="reldef:rec:sweetness"
def "reldef:regulation:subject_to" "regulation subject to — regulation relation (regulation subject to)."
set.add name="rels" id="reldef:regulation:subject_to"
def "reldef:religion:branch_of" "branch of — the tradition is a branch of the target religion."
set.add name="rels" id="reldef:religion:branch_of"
def "reldef:religion:originated_in" "originated in — the religion originated in the target place."
set.add name="rels" id="reldef:religion:originated_in"
def "reldef:religion:sacred_to" "sacred to — the item is sacred to the target religion."
set.add name="rels" id="reldef:religion:sacred_to"
def "reldef:school:sits" "school sits — school relation (school sits)."
set.add name="rels" id="reldef:school:sits"
def "reldef:school:stage" "school stage — school relation (school stage)."
set.add name="rels" id="reldef:school:stage"
def "reldef:sci_hist:achieved_in" "achieved in — the scientific achievement was made in the target era."
set.add name="rels" id="reldef:sci_hist:achieved_in"
def "reldef:sci_hist:discovered_by" "discovered by — the discovery was made by the target person."
set.add name="rels" id="reldef:sci_hist:discovered_by"
def "reldef:seasonmap:north" "seasonmap north — seasonmap relation (seasonmap north)."
set.add name="rels" id="reldef:seasonmap:north"
def "reldef:seasonmap:south" "seasonmap south — seasonmap relation (seasonmap south)."
set.add name="rels" id="reldef:seasonmap:south"
def "reldef:social:member_of" "social member of — social relation (social member of)."
set.add name="rels" id="reldef:social:member_of"
def "reldef:social:rel_kind" "social rel kind — social relation (social rel kind)."
set.add name="rels" id="reldef:social:rel_kind"
def "reldef:social:rel_polarity" "social rel polarity — social relation (social rel polarity)."
set.add name="rels" id="reldef:social:rel_polarity"
def "reldef:status:outranks" "status outranks — status relation (status outranks)."
set.add name="rels" id="reldef:status:outranks"
def "reldef:sys:abbreviation_of" "abbreviation of — the source is a short form of the target."
set.add name="rels" id="reldef:sys:abbreviation_of"
def "reldef:sys:antonym" "antonym — the two concepts are opposite in meaning."
set.add name="rels" id="reldef:sys:antonym"
def "reldef:sys:associated_with" "associated with — the source is commonly associated with the target."
set.add name="rels" id="reldef:sys:associated_with"
def "reldef:sys:capital_of" "capital of — the source city is the capital of the target place."
set.add name="rels" id="reldef:sys:capital_of"
def "reldef:sys:has_capital" "has capital — the target city is the capital of the source place."
set.add name="rels" id="reldef:sys:has_capital"
def "reldef:sys:is_a" "is a — the source is a kind or instance of the target (taxonomic type)."
set.add name="rels" id="reldef:sys:is_a"
def "reldef:sys:part_of" "part of — the source is a component or member of the target whole."
set.add name="rels" id="reldef:sys:part_of"
def "reldef:sys:refers_to" "refers to — the source atom refers to the target proto-word or concept."
set.add name="rels" id="reldef:sys:refers_to"
def "reldef:sys:related_to" "related to — a general association between the two concepts."
set.add name="rels" id="reldef:sys:related_to"
def "reldef:sys:same_as" "same as — the two atoms denote the same thing."
set.add name="rels" id="reldef:sys:same_as"
def "reldef:sys:source" "source — the target is a source or origin of the source atom."
set.add name="rels" id="reldef:sys:source"
def "reldef:sys:synonym" "synonym — the two concepts share the same meaning."
set.add name="rels" id="reldef:sys:synonym"
def "reldef:tcm:action" "tcm action — tcm relation (tcm action)."
set.add name="rels" id="reldef:tcm:action"
def "reldef:tcm:meridian" "tcm meridian — tcm relation (tcm meridian)."
set.add name="rels" id="reldef:tcm:meridian"
def "reldef:tcm:nature" "tcm nature — tcm relation (tcm nature)."
set.add name="rels" id="reldef:tcm:nature"
def "reldef:tcm:recommends_action" "tcm recommends action — tcm relation (tcm recommends action)."
set.add name="rels" id="reldef:tcm:recommends_action"
def "reldef:tcm:recommends_nature" "tcm recommends nature — tcm relation (tcm recommends nature)."
set.add name="rels" id="reldef:tcm:recommends_nature"
def "reldef:technique:heat_source" "technique heat source — technique relation (technique heat source)."
set.add name="rels" id="reldef:technique:heat_source"
def "reldef:technique:param" "technique param — technique relation (technique param)."
set.add name="rels" id="reldef:technique:param"
def "reldef:technique:variant_of" "technique variant of — technique relation (technique variant of)."
set.add name="rels" id="reldef:technique:variant_of"
def "reldef:thesaurus:antonym" "antonym — the two words are opposite in sense."
set.add name="rels" id="reldef:thesaurus:antonym"
def "reldef:thesaurus:external_ref" "external reference — links the concept to an external record or resource."
set.add name="rels" id="reldef:thesaurus:external_ref"
def "reldef:thesaurus:hypernym" "hypernym — the target is a broader category of the source (is-a upward)."
set.add name="rels" id="reldef:thesaurus:hypernym"
def "reldef:thesaurus:hyponym" "hyponym — the target is a narrower kind of the source (is-a downward)."
set.add name="rels" id="reldef:thesaurus:hyponym"
def "reldef:thesaurus:related" "related — links two concepts that share meaning (thesaurus relation)."
set.add name="rels" id="reldef:thesaurus:related"
def "reldef:thesaurus:synonym" "synonym — the two words share the same sense."
set.add name="rels" id="reldef:thesaurus:synonym"
def "reldef:verb:act:carry" "verb act carry — verb relation (verb act carry)."
set.add name="rels" id="reldef:verb:act:carry"
def "reldef:verb:acts_on" "acts on — the verb acts on the target object."
set.add name="rels" id="reldef:verb:acts_on"
def "reldef:work:belong_via" "work belong via — work relation (work belong via)."
set.add name="rels" id="reldef:work:belong_via"
