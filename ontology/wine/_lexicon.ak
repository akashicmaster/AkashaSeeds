# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.


# ── namespaces introduced by this pack ──
def "nsdef:cacao" "cacao — the cacao namespace."
set.add name="namespaces" id="nsdef:cacao"
def "nsdef:glassware" "glassware — the glassware namespace."
set.add name="namespaces" id="nsdef:glassware"
def "nsdef:taste" "taste — the taste namespace."
set.add name="namespaces" id="nsdef:taste"

# ── relations introduced by this pack ──
def "reldef:beer:style" "beer style — beer relation (beer style)."
set.add name="rels" id="reldef:beer:style"
def "reldef:beverage:kind" "beverage kind — beverage relation (beverage kind)."
set.add name="rels" id="reldef:beverage:kind"
def "reldef:beverage:made_from" "beverage made from — beverage relation (beverage made from)."
set.add name="rels" id="reldef:beverage:made_from"
def "reldef:cheese:made_from" "cheese made from — cheese relation (cheese made from)."
set.add name="rels" id="reldef:cheese:made_from"
def "reldef:cheese:origin_country" "cheese origin country — cheese relation (cheese origin country)."
set.add name="rels" id="reldef:cheese:origin_country"
def "reldef:chocolate:made_from" "chocolate made from — chocolate relation (chocolate made from)."
set.add name="rels" id="reldef:chocolate:made_from"
def "reldef:cocktail:base" "cocktail base — cocktail relation (cocktail base)."
set.add name="rels" id="reldef:cocktail:base"
def "reldef:cocktail:garnished_with" "cocktail garnished with — cocktail relation (cocktail garnished with)."
set.add name="rels" id="reldef:cocktail:garnished_with"
def "reldef:cocktail:mixed" "cocktail mixed — cocktail relation (cocktail mixed)."
set.add name="rels" id="reldef:cocktail:mixed"
def "reldef:cocktail:served_in" "cocktail served in — cocktail relation (cocktail served in)."
set.add name="rels" id="reldef:cocktail:served_in"
def "reldef:cocktail:strength" "cocktail strength — cocktail relation (cocktail strength)."
set.add name="rels" id="reldef:cocktail:strength"
def "reldef:cocktail:tastes" "cocktail tastes — cocktail relation (cocktail tastes)."
set.add name="rels" id="reldef:cocktail:tastes"
def "reldef:coffee:origin" "coffee origin — coffee relation (coffee origin)."
set.add name="rels" id="reldef:coffee:origin"
def "reldef:garnish:kind" "garnish kind — garnish relation (garnish kind)."
set.add name="rels" id="reldef:garnish:kind"
def "reldef:liqueur:is" "liqueur is — liqueur relation (liqueur is)."
set.add name="rels" id="reldef:liqueur:is"
def "reldef:liqueur:kind" "liqueur kind — liqueur relation (liqueur kind)."
set.add name="rels" id="reldef:liqueur:kind"
def "reldef:pairing:accord" "pairing accord — pairing relation (pairing accord)."
set.add name="rels" id="reldef:pairing:accord"
def "reldef:pairing:contraste" "pairing contraste — pairing relation (pairing contraste)."
set.add name="rels" id="reldef:pairing:contraste"
def "reldef:pairing:regional_pairing" "pairing regional pairing — pairing relation (pairing regional pairing)."
set.add name="rels" id="reldef:pairing:regional_pairing"
def "reldef:pairing:sweetness_with_saltiness" "pairing sweetness with saltiness — pairing relation (pairing sweetness with saltiness)."
set.add name="rels" id="reldef:pairing:sweetness_with_saltiness"
def "reldef:sake:grade" "sake grade — sake relation (sake grade)."
set.add name="rels" id="reldef:sake:grade"
def "reldef:spirit:type" "spirit type — spirit relation (spirit type)."
set.add name="rels" id="reldef:spirit:type"
def "reldef:tea:from" "tea from — tea relation (tea from)."
set.add name="rels" id="reldef:tea:from"
def "reldef:whiskey:type" "whiskey type — whiskey relation (whiskey type)."
set.add name="rels" id="reldef:whiskey:type"
def "reldef:wine:located_in" "wine located in — wine relation (wine located in)."
set.add name="rels" id="reldef:wine:located_in"
def "reldef:wine:originated_in" "wine originated in — wine relation (wine originated in)."
set.add name="rels" id="reldef:wine:originated_in"
def "reldef:wine:produced_in" "wine produced in — wine relation (wine produced in)."
set.add name="rels" id="reldef:wine:produced_in"
