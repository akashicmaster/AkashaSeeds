# Generated by scripts/gen_lexicon.py — relation / namespace definition layer.
# See docs/for-llm/rel-definition-spec.md. Do not edit by hand; re-run the generator.


# ── namespaces introduced by this pack ──
def "nsdef:bdg" "bdg — the bdg namespace."
set.add name="namespaces" id="nsdef:bdg"
def "nsdef:cockpit" "cockpit — the cockpit namespace."
set.add name="namespaces" id="nsdef:cockpit"
def "nsdef:corr" "corr — the corr namespace."
set.add name="namespaces" id="nsdef:corr"
def "nsdef:game" "game — the game namespace."
set.add name="namespaces" id="nsdef:game"
def "nsdef:maas" "maas — the maas namespace."
set.add name="namespaces" id="nsdef:maas"
def "nsdef:mystic" "mystic — the mystic namespace."
set.add name="namespaces" id="nsdef:mystic"
def "nsdef:off" "off — the off namespace."
set.add name="namespaces" id="nsdef:off"
def "nsdef:pa" "pa — the pa namespace."
set.add name="namespaces" id="nsdef:pa"
def "nsdef:pres" "pres — the pres namespace."
set.add name="namespaces" id="nsdef:pres"
def "nsdef:process" "process — the process namespace."
set.add name="namespaces" id="nsdef:process"
def "nsdef:prop" "prop — the prop namespace."
set.add name="namespaces" id="nsdef:prop"
def "nsdef:ptd" "ptd — the ptd namespace."
set.add name="namespaces" id="nsdef:ptd"
def "nsdef:scm" "scm — the scm namespace."
set.add name="namespaces" id="nsdef:scm"
def "nsdef:wr" "wr — the wr namespace."
set.add name="namespaces" id="nsdef:wr"

# ── relations introduced by this pack ──
def "reldef:@accepts" "accepts — 'accepts' relation asserted between two atoms."
set.add name="rels" id="reldef:@accepts"
def "reldef:@achieves" "achieves — 'achieves' relation asserted between two atoms."
set.add name="rels" id="reldef:@achieves"
def "reldef:@activates" "activates — 'activates' relation asserted between two atoms."
set.add name="rels" id="reldef:@activates"
def "reldef:@acts_as" "acts as — 'acts as' relation asserted between two atoms."
set.add name="rels" id="reldef:@acts_as"
def "reldef:@adapted_into" "adapted into — 'adapted into' relation asserted between two atoms."
set.add name="rels" id="reldef:@adapted_into"
def "reldef:@affects" "affects — 'affects' relation asserted between two atoms."
set.add name="rels" id="reldef:@affects"
def "reldef:@amplifies" "amplifies — 'amplifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@amplifies"
def "reldef:@analyzes" "analyzes — 'analyzes' relation asserted between two atoms."
set.add name="rels" id="reldef:@analyzes"
def "reldef:@assigned_to" "assigned to — 'assigned to' relation asserted between two atoms."
set.add name="rels" id="reldef:@assigned_to"
def "reldef:@assists" "assists — 'assists' relation asserted between two atoms."
set.add name="rels" id="reldef:@assists"
def "reldef:@authorizes" "authorizes — 'authorizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@authorizes"
def "reldef:@based_on" "based on — 'based on' relation asserted between two atoms."
set.add name="rels" id="reldef:@based_on"
def "reldef:@benefits_from" "benefits from — 'benefits from' relation asserted between two atoms."
set.add name="rels" id="reldef:@benefits_from"
def "reldef:@binds_to" "binds to — 'binds to' relation asserted between two atoms."
set.add name="rels" id="reldef:@binds_to"
def "reldef:@blocks" "blocks — 'blocks' relation asserted between two atoms."
set.add name="rels" id="reldef:@blocks"
def "reldef:@blocks_effect_of" "blocks effect of — 'blocks effect of' relation asserted between two atoms."
set.add name="rels" id="reldef:@blocks_effect_of"
def "reldef:@breaks" "breaks — 'breaks' relation asserted between two atoms."
set.add name="rels" id="reldef:@breaks"
def "reldef:@bypasses" "bypasses — 'bypasses' relation asserted between two atoms."
set.add name="rels" id="reldef:@bypasses"
def "reldef:@can_distort" "can distort — 'can distort' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_distort"
def "reldef:@can_enable" "can enable — 'can enable' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_enable"
def "reldef:@can_perform" "can perform — 'can perform' relation asserted between two atoms."
set.add name="rels" id="reldef:@can_perform"
def "reldef:@catalyzes" "catalyzes — 'catalyzes' relation asserted between two atoms."
set.add name="rels" id="reldef:@catalyzes"
def "reldef:@causes" "causes — 'causes' relation asserted between two atoms."
set.add name="rels" id="reldef:@causes"
def "reldef:@central_concept_of" "central concept of — 'central concept of' relation asserted between two atoms."
set.add name="rels" id="reldef:@central_concept_of"
def "reldef:@changes" "changes — 'changes' relation asserted between two atoms."
set.add name="rels" id="reldef:@changes"
def "reldef:@charges" "charges — 'charges' relation asserted between two atoms."
set.add name="rels" id="reldef:@charges"
def "reldef:@clarifies" "clarifies — 'clarifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@clarifies"
def "reldef:@classified_as" "classified as — 'classified as' relation asserted between two atoms."
set.add name="rels" id="reldef:@classified_as"
def "reldef:@codes_for_type" "codes for type — 'codes for type' relation asserted between two atoms."
set.add name="rels" id="reldef:@codes_for_type"
def "reldef:@comes_from" "comes from — 'comes from' relation asserted between two atoms."
set.add name="rels" id="reldef:@comes_from"
def "reldef:@communicates" "communicates — 'communicates' relation asserted between two atoms."
set.add name="rels" id="reldef:@communicates"
def "reldef:@competes_for" "competes for — 'competes for' relation asserted between two atoms."
set.add name="rels" id="reldef:@competes_for"
def "reldef:@complements" "complements — 'complements' relation asserted between two atoms."
set.add name="rels" id="reldef:@complements"
def "reldef:@concerns" "concerns — 'concerns' relation asserted between two atoms."
set.add name="rels" id="reldef:@concerns"
def "reldef:@confirms" "confirms — 'confirms' relation asserted between two atoms."
set.add name="rels" id="reldef:@confirms"
def "reldef:@connects" "connects — 'connects' relation asserted between two atoms."
set.add name="rels" id="reldef:@connects"
def "reldef:@constrains" "constrains — 'constrains' relation asserted between two atoms."
set.add name="rels" id="reldef:@constrains"
def "reldef:@contrasts_with_in_approach" "contrasts with in approach — 'contrasts with in approach' relation asserted between two atoms."
set.add name="rels" id="reldef:@contrasts_with_in_approach"
def "reldef:@controlled_by" "controlled by — 'controlled by' relation asserted between two atoms."
set.add name="rels" id="reldef:@controlled_by"
def "reldef:@coordinates" "coordinates — 'coordinates' relation asserted between two atoms."
set.add name="rels" id="reldef:@coordinates"
def "reldef:@decreases" "decreases — 'decreases' relation asserted between two atoms."
set.add name="rels" id="reldef:@decreases"
def "reldef:@defined_by" "defined by — 'defined by' relation asserted between two atoms."
set.add name="rels" id="reldef:@defined_by"
def "reldef:@defines" "defines — 'defines' relation asserted between two atoms."
set.add name="rels" id="reldef:@defines"
def "reldef:@departs_from" "departs from — 'departs from' relation asserted between two atoms."
set.add name="rels" id="reldef:@departs_from"
def "reldef:@describes" "describes — 'describes' relation asserted between two atoms."
set.add name="rels" id="reldef:@describes"
def "reldef:@detects" "detects — 'detects' relation asserted between two atoms."
set.add name="rels" id="reldef:@detects"
def "reldef:@determines" "determines — 'determines' relation asserted between two atoms."
set.add name="rels" id="reldef:@determines"
def "reldef:@disrupts" "disrupts — 'disrupts' relation asserted between two atoms."
set.add name="rels" id="reldef:@disrupts"
def "reldef:@distorts" "distorts — 'distorts' relation asserted between two atoms."
set.add name="rels" id="reldef:@distorts"
def "reldef:@documents" "documents — 'documents' relation asserted between two atoms."
set.add name="rels" id="reldef:@documents"
def "reldef:@donates" "donates — 'donates' relation asserted between two atoms."
set.add name="rels" id="reldef:@donates"
def "reldef:@driven_by" "driven by — 'driven by' relation asserted between two atoms."
set.add name="rels" id="reldef:@driven_by"
def "reldef:@drives" "drives — 'drives' relation asserted between two atoms."
set.add name="rels" id="reldef:@drives"
def "reldef:@emerges_from" "emerges from — 'emerges from' relation asserted between two atoms."
set.add name="rels" id="reldef:@emerges_from"
def "reldef:@enables_labor_force_for" "enables labor force for — 'enables labor force for' relation asserted between two atoms."
set.add name="rels" id="reldef:@enables_labor_force_for"
def "reldef:@expresses" "expresses — 'expresses' relation asserted between two atoms."
set.add name="rels" id="reldef:@expresses"
def "reldef:@feeds" "feeds — 'feeds' relation asserted between two atoms."
set.add name="rels" id="reldef:@feeds"
def "reldef:@forms" "forms — 'forms' relation asserted between two atoms."
set.add name="rels" id="reldef:@forms"
def "reldef:@foundational_ethics_for" "foundational ethics for — 'foundational ethics for' relation asserted between two atoms."
set.add name="rels" id="reldef:@foundational_ethics_for"
def "reldef:@from" "from — 'from' relation asserted between two atoms."
set.add name="rels" id="reldef:@from"
def "reldef:@generates" "generates — 'generates' relation asserted between two atoms."
set.add name="rels" id="reldef:@generates"
def "reldef:@governs" "governs — 'governs' relation asserted between two atoms."
set.add name="rels" id="reldef:@governs"
def "reldef:@guides" "guides — 'guides' relation asserted between two atoms."
set.add name="rels" id="reldef:@guides"
def "reldef:@has_property" "has property — 'has property' relation asserted between two atoms."
set.add name="rels" id="reldef:@has_property"
def "reldef:@hosts" "hosts — 'hosts' relation asserted between two atoms."
set.add name="rels" id="reldef:@hosts"
def "reldef:@identifies" "identifies — 'identifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@identifies"
def "reldef:@imparts" "imparts — 'imparts' relation asserted between two atoms."
set.add name="rels" id="reldef:@imparts"
def "reldef:@improves" "improves — 'improves' relation asserted between two atoms."
set.add name="rels" id="reldef:@improves"
def "reldef:@includes" "includes — 'includes' relation asserted between two atoms."
set.add name="rels" id="reldef:@includes"
def "reldef:@increases_availability_of" "increases availability of — 'increases availability of' relation asserted between two atoms."
set.add name="rels" id="reldef:@increases_availability_of"
def "reldef:@informed_by" "informed by — 'informed by' relation asserted between two atoms."
set.add name="rels" id="reldef:@informed_by"
def "reldef:@informs" "informs — 'informs' relation asserted between two atoms."
set.add name="rels" id="reldef:@informs"
def "reldef:@inhibits" "inhibits — 'inhibits' relation asserted between two atoms."
set.add name="rels" id="reldef:@inhibits"
def "reldef:@integrates_with" "integrates with — 'integrates with' relation asserted between two atoms."
set.add name="rels" id="reldef:@integrates_with"
def "reldef:@intensifies" "intensifies — 'intensifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@intensifies"
def "reldef:@is_a_type_of" "is a type of — 'is a type of' relation asserted between two atoms."
set.add name="rels" id="reldef:@is_a_type_of"
def "reldef:@limits" "limits — 'limits' relation asserted between two atoms."
set.add name="rels" id="reldef:@limits"
def "reldef:@made_by" "made by — 'made by' relation asserted between two atoms."
set.add name="rels" id="reldef:@made_by"
def "reldef:@maintains" "maintains — 'maintains' relation asserted between two atoms."
set.add name="rels" id="reldef:@maintains"
def "reldef:@manages" "manages — 'manages' relation asserted between two atoms."
set.add name="rels" id="reldef:@manages"
def "reldef:@maps_to_body_parts" "maps to body parts — 'maps to body parts' relation asserted between two atoms."
set.add name="rels" id="reldef:@maps_to_body_parts"
def "reldef:@may_be_hidden_by" "may be hidden by — 'may be hidden by' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_be_hidden_by"
def "reldef:@may_become" "may become — 'may become' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_become"
def "reldef:@may_cause" "may cause — 'may cause' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_cause"
def "reldef:@may_change" "may change — 'may change' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_change"
def "reldef:@may_create" "may create — 'may create' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_create"
def "reldef:@may_mentor" "may mentor — 'may mentor' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_mentor"
def "reldef:@may_respond_to" "may respond to — 'may respond to' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_respond_to"
def "reldef:@may_trigger" "may trigger — 'may trigger' relation asserted between two atoms."
set.add name="rels" id="reldef:@may_trigger"
def "reldef:@measures" "measures — 'measures' relation asserted between two atoms."
set.add name="rels" id="reldef:@measures"
def "reldef:@mitigates" "mitigates — 'mitigates' relation asserted between two atoms."
set.add name="rels" id="reldef:@mitigates"
def "reldef:@monitored_by" "monitored by — 'monitored by' relation asserted between two atoms."
set.add name="rels" id="reldef:@monitored_by"
def "reldef:@observes" "observes — 'observes' relation asserted between two atoms."
set.add name="rels" id="reldef:@observes"
def "reldef:@often_is" "often is — 'often is' relation asserted between two atoms."
set.add name="rels" id="reldef:@often_is"
def "reldef:@opposes" "opposes — 'opposes' relation asserted between two atoms."
set.add name="rels" id="reldef:@opposes"
def "reldef:@organizes" "organizes — 'organizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@organizes"
def "reldef:@part_of" "part of — 'part of' relation asserted between two atoms."
set.add name="rels" id="reldef:@part_of"
def "reldef:@participates_in" "participates in — 'participates in' relation asserted between two atoms."
set.add name="rels" id="reldef:@participates_in"
def "reldef:@polymerizes_to" "polymerizes to — 'polymerizes to' relation asserted between two atoms."
set.add name="rels" id="reldef:@polymerizes_to"
def "reldef:@precursor_of" "precursor of — 'precursor of' relation asserted between two atoms."
set.add name="rels" id="reldef:@precursor_of"
def "reldef:@predicts" "predicts — 'predicts' relation asserted between two atoms."
set.add name="rels" id="reldef:@predicts"
def "reldef:@pressures" "pressures — 'pressures' relation asserted between two atoms."
set.add name="rels" id="reldef:@pressures"
def "reldef:@provides_efficiently" "provides efficiently — 'provides efficiently' relation asserted between two atoms."
set.add name="rels" id="reldef:@provides_efficiently"
def "reldef:@quantifies" "quantifies — 'quantifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@quantifies"
def "reldef:@reads" "reads — 'reads' relation asserted between two atoms."
set.add name="rels" id="reldef:@reads"
def "reldef:@reduces_synthesis_of" "reduces synthesis of — 'reduces synthesis of' relation asserted between two atoms."
set.add name="rels" id="reldef:@reduces_synthesis_of"
def "reldef:@refers_to" "refers to — 'refers to' relation asserted between two atoms."
set.add name="rels" id="reldef:@refers_to"
def "reldef:@regulates_holistically" "regulates holistically — 'regulates holistically' relation asserted between two atoms."
set.add name="rels" id="reldef:@regulates_holistically"
def "reldef:@regulates_uptake_of" "regulates uptake of — 'regulates uptake of' relation asserted between two atoms."
set.add name="rels" id="reldef:@regulates_uptake_of"
def "reldef:@relates_to" "relates to — 'relates to' relation asserted between two atoms."
set.add name="rels" id="reldef:@relates_to"
def "reldef:@releases" "releases — 'releases' relation asserted between two atoms."
set.add name="rels" id="reldef:@releases"
def "reldef:@replaced_by" "replaced by — 'replaced by' relation asserted between two atoms."
set.add name="rels" id="reldef:@replaced_by"
def "reldef:@required_for" "required for — 'required for' relation asserted between two atoms."
set.add name="rels" id="reldef:@required_for"
def "reldef:@requires_attention" "requires attention — 'requires attention' relation asserted between two atoms."
set.add name="rels" id="reldef:@requires_attention"
def "reldef:@responsible_for" "responsible for — 'responsible for' relation asserted between two atoms."
set.add name="rels" id="reldef:@responsible_for"
def "reldef:@restores" "restores — 'restores' relation asserted between two atoms."
set.add name="rels" id="reldef:@restores"
def "reldef:@secretes" "secretes — 'secretes' relation asserted between two atoms."
set.add name="rels" id="reldef:@secretes"
def "reldef:@specializes" "specializes — 'specializes' relation asserted between two atoms."
set.add name="rels" id="reldef:@specializes"
def "reldef:@stabilizes" "stabilizes — 'stabilizes' relation asserted between two atoms."
set.add name="rels" id="reldef:@stabilizes"
def "reldef:@stimulates_release_of" "stimulates release of — 'stimulates release of' relation asserted between two atoms."
set.add name="rels" id="reldef:@stimulates_release_of"
def "reldef:@strengthens" "strengthens — 'strengthens' relation asserted between two atoms."
set.add name="rels" id="reldef:@strengthens"
def "reldef:@targets" "targets — 'targets' relation asserted between two atoms."
set.add name="rels" id="reldef:@targets"
def "reldef:@to" "to — 'to' relation asserted between two atoms."
set.add name="rels" id="reldef:@to"
def "reldef:@transports_type" "transports type — 'transports type' relation asserted between two atoms."
set.add name="rels" id="reldef:@transports_type"
def "reldef:@unifies" "unifies — 'unifies' relation asserted between two atoms."
set.add name="rels" id="reldef:@unifies"
def "reldef:@used_in" "used in — 'used in' relation asserted between two atoms."
set.add name="rels" id="reldef:@used_in"
